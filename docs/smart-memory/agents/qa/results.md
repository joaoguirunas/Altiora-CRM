---
title: QA Results
type: qa-result
agent: dev-qa
updated: 2026-07-25
tags: [qa, results, gates, user-types, nylas, webhook-increment, webhook-template-fix, squad-d-review, squad-a-removal, squad-b-omni, agentes-ia, diagnostico, kiwify, sends-pro, clean, arch-rbac, cron]
related: ["[[agent-save-409-review]]", "[[../../stories/active/AUTH-V2-03c]]", "[[../../stories/done/AUTH-V2-01]]", "[[../../stories/done/AUTH-V2-03c-fixup]]", "[[../../stories/done/BI-VOICE-00]]", "[[../../stories/done/BI-VOICE-01]]", "[[../../stories/done/BI-VOICE-02]]", "[[../../stories/backlog/BI-VOICE-04]]", "[[../../stories/backlog/SENDS-IMPORT-02]]", "[[../../stories/backlog/FIX-SENDS-01]]"]
---

# QA Results

Histórico cross-story de veredictos formais emitidos por Axikar (dev-qa).
Veredictos completos são preenchidos na seção `## QA Results` da própria story.

## Tabela

| Story        | Data       | Veredicto    | Issues                                                                                  | Agente              |
|---           |---         |---           |---                                                                                       |---                   |
| REL-03 (AC1+AC3+AC4 DB — adm_client_drift + pg_cron drift-check-daily + compute_schema_hash REVOKE PUBLIC) | 2026-07-25 | **✅ PASS** | AC1: adm_client_drift schema completo (id uuid PK, client_id FK CASCADE, expected/actual hash, status CHECK 3 estados, repaired_by FK SET NULL). 2 índices (client_detected DESC, status WHERE detected). RLS super_admin+service_role. AC3: cron 0 4 * * *, GUC app.supabase_url+app.service_role_key, cron.unschedule idempotente, fora transação. AC4: compute_schema_hash SECURITY DEFINER, 6 categorias ORDER BY, REVOKE ALL FROM PUBLIC, GRANT service_role apenas. Rollbacks ✅. AC2/AC5-AC9 aguardam dev-beta. | Bythak (dev-data-engineer) — Axikar gate |
| bi-4 FINAL PASS (BIProRevOpsTab+BIProMarketingTab enterprise redesign — CONCERNS resolvidos) | 2026-07-25 | **✅ PASS** (v2 final) | CONCERN-1: aria-expanded+aria-controls+id cr-diagnosis-{key} em RevOpsTab L526-537 ✅. CONCERN-2: MarketingTab L104 text-[11px] (era text-[10px]), zero remanescentes ✅. CONCERN-3: aria-label em Edit/Save/Cancel benchmarks L432/L440/L449 ✅. tsc EXIT 0, eslint 0 errors. | Serak (dev-dev-gamma) — Axikar gate |
| bi-3 FINAL PASS (BIProComercialTab enterprise redesign — CONCERNS resolvidos) | 2026-07-25 | **✅ PASS** (v2 final) | CONCERN-1: role="button" aria-expanded aria-controls closer-detail-{userId} em CloserTable L917-919, id L959 ✅. CONCERN-2: caption sr-only nas 3 tabelas L433/L723/L851 ✅. CONCERN-3: ChevronDown rotation -rotate-90/rotate-0 transition-transform duration-200 L928-931 ✅. tsc EXIT 0, eslint 0 errors. | Serak (dev-dev-gamma) — Axikar gate |
| OBS-DISPATCH-HEALTH-01 AC4+AC5 (DispatchHealthCard — 3 LEDs cron/canal/queue, collapsible, Disparos.tsx+DisparoDetalhes.tsx) | 2026-07-25 | **✅ PASS** (v2) | AC4: 3 LEDs derivados (deriveCronLed/deriveChannelLed/deriveQueueLed). Collapsible+aria-expanded. useDispatchHealthView (v_dispatch_health, 30s poll). useSendHealth (get_send_health RPC, enabled somente em running/paused). Graceful 42501→[]/null. AC5: Disparos.tsx kpis.running>0 guard. DisparoDetalhes.tsx sendId+sendStatus. Lazy: RPC disabled global. tsc EXIT 0. CONCERN-1/2/3 DB herdados de v1. AC6 smoke ⏳ pós-deploy. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-SENDS-FIRST-MSG-01 (AC8+AC9 DB — tabela message_delivery_attempts + 2 índices + RLS espelhando messages) | 2026-07-25 | **✅ PASS** | AC8: bigserial PK, message_id FK CASCADE, attempt_no, channel, status CHECK 4 estados, request_body/response_body jsonb, duration_ms GENERATED, COMMENT ON TABLE+COLUMN. AC9: idx_mda_message_id_attempt(message_id,attempt_no) + idx_mda_status_started(status,started_at DESC). RLS: authenticated_read+authenticated_write USING(true), mirrors 20260428060000. Sequence grant. Rollback ✅. | Bythak (dev-data-engineer) — Axikar gate |
| FIX-BI-01 (OAuth token refresh Meta Ads + TikTok sync edge fn localizada) | 2026-07-25 | **⚠️ CONCERNS** | AC1 Meta: fb_exchange_token proativo 7d threshold + tokenExpired:true 401. Google pré-existente. AC2 TikTok: tiktok-ads-sync/index.ts refreshTikTokAdsToken() + pg_cron 6h. AC3 handleMetaSync filtra contas expiradas. AC4 banner persistente vermelho/âmbar. [CONCERN-1 LOW] permissão admin/manager verificada APÓS bloco de token extension (L154 vs L97) — qualquer user autenticado pode disparar fb_exchange_token. Não-destrutivo. Sugere story hardening. Push liberado. | Serak (dev-dev-gamma) — Axikar gate |
| REL-02 (ADM Atualizar Cliente UI — badge versão + modal changelog + Realtime progress + bulk 5-concurrent) | 2026-07-25 | **✅ PASS** | 8/8 ACs. tsc EXIT 0. AC1 badge 3 estados. AC2 useAdmReleasesBetween+Confirmo checkbox. AC3 Realtime adm-sync-job+useAdmSyncLogs. AC4 BulkUpdateModal max5 concurrent. AC5 ClientVersionsHistory. AC6 useUpdateClient+useBulkUpdateClients+insertAuditLog. AC7 localStorage lastSeen toast. AC8 Dialog focus trap+aria-busy+role=status. | Novik (dev-dev-alpha) — Axikar gate |
| FUP-AUTO-01 (completo: DB+WORKER+TOOL+UI) | 2026-07-25 | **⚠️ CONCERNS** | DB ✅ (v1 pass). WORKER-1 ✅ 3 tipos+MAX_RETRIES=3+status flow. UI-1 ✅ ScheduledCallbacksTab+useFupProgramados+refetchInterval:60s. [CONCERN-1 MEDIUM] TOOL-1: agendar_fup em CALLBACK_TOOL_DEFINITIONS (condicional callbackConfig.enabled) vs spec "TOOL_DEFINITIONS" (sempre disponível). Push liberado. Lead confirmar se escopo está correto. | Bythak+Serak (dev-dev-gamma) — Axikar gate |
| FUP-AUTO-01 (DB: tabela fup_programados + 4 índices + RLS + agendar_fup() SECURITY DEFINER + cron */5) | 2026-07-25 | **✅ PASS** | DB-1 schema completo (13 colunas, FKs CASCADE/SET NULL, soft delete, trigger updated_at). DB-2 4 índices parciais IF NOT EXISTS. DB-3 RLS 4 policies (SELECT auth, INSERT/UPDATE admin+manager, ALL service_role). DB-4 SECURITY DEFINER + REVOKE ALL FROM PUBLIC + validações tipo+scheduled+lead. DB-5 cron fora transação, unschedule+schedule idempotente, fn_cron_http_call. | Bythak (dev-data-engineer) — Axikar gate |
| REL-01 AC7 (sync-clients.yml workflow_dispatch only — breaking change documentada + SYNC_REASON audit) | 2026-07-25 | **✅ PASS** (v3) | workflow_dispatch ONLY (zero push trigger). Breaking change documentada em header extenso. reason input REQUIRED + validação extra exit 1. TRIGGERED_BY+SYNC_REASON+TARGET_VERSION em sync-clients.js. Audit log step no início. Tenant updates agora opt-in via UI (REL-02). | dev-devops — Axikar gate |
| REL-04 (Migration Discipline — lint-migrations.js MIG001-9 + lint-migrations.yml + dry-run + backfill) | 2026-07-25 | **✅ PASS** (v2) | AC1-AC6 ✅. MIG009 manifest check. lint-migrations.yml exit 1 + PR comment. dry-run Postgres 15 container (limitação documentada: sem extensões Supabase). 902 arquivos / 1.801 erros mapeados no baseline. AC7 opcional. | Bythak+devops — Axikar gate |
| REL-01 (Versioned Releases — release-tag.yml + adm_releases migrations + adm-releases-register) | 2026-07-25 | **✅ PASS** (v2 — AC2+AC3+AC4) | AC2: release-tag.yml trigger migrations→main, bump version, gera release.json, tag, POST adm-releases-register. AC3: 3 migrations (adm_releases+adm_client_versions+adm_clients_version_columns), todas IF EXISTS + rollback. AC4: edge fn service_role auth + ON CONFLICT + adm_audit_log. AC5/6/7/8 aguardam dev-beta+devops. | Bythak+devops — Axikar gate |
| FIX-SENDS-FE-VALIDATION (3 gaps FE↔BE SENDS PRO: template filter + vars_map block + list activation) | 2026-07-25 | **⚠️ CONCERNS** | AC1 ✅ filtro 3 condições (system_enabled+meta_template_name+APPROVED). AC2 ✅ extractTemplateVars+validate()→false. AC3 ✅ handleAtivar/Retomar→startFirstBatch+Loader2+rollback. AC5 WAIVED. AC6 tsc 0. [PENDING] AC4.3: smoke canal sem access_token requer runtime — verificado via código, não executado. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-SENDS-IMPORT-04 (Dedup + bulk insert >1000 contatos — BATCH_SIZE=500, 5 fases bulk, caches, timeout) | 2026-07-25 | **✅ PASS** | 6/6 ACs. BATCH_SIZE=500; 5 fases bulk/batch; scoreMatrixCache+companyCache; TIMEOUT_MS=130_000+timedOut→failed; ~20s/5000 contatos vs 80s+ anterior. ✅ | Rex (dev-dev-beta) — Axikar gate |
| FIX-SENDS-IMPORT-03 (Criar lead para contatos existentes quando create_leads=true) | 2026-07-25 | **✅ PASS** | 6/6 ACs. Bloco create_leads && pipeline_id em existingPersonId L624 antes do continue. Dedup lead por people_id+pipeline_id. lead_extra aplicado. Contadores intocados. ✅ | Rex (dev-dev-beta) — Axikar gate |
| FIX-ADM-01 (Rollback em create-global-user — rollbackAuthUser + authUserCreated + 422 graceful) | 2026-07-25 | **✅ PASS** | 4/4 ACs. AC2+AC4 N/A standalone (sem adm_clients/secret_hint). rollbackAuthUser() L40; authUserCreated L158; profileCreateError→rollback+422 L267-276. Zero órfãos. ✅ | Rex (dev-dev-beta) — Axikar gate |
| SENDS-FIX-01 (Auditoria adversarial SENDS PRO — 406 linhas, estado AMARELO, 5 findings P1/P2) | 2026-07-25 | **✅ PASS** | 8/8 ACs. audit-sends-pro.md (406 linhas, commit 23f7fd60). 5 vetores cobertos. Snippets P1 com file:linha. Reproduções verificadas. Tabela regressões. Débito catalogado. Sumário AMARELO. ✅ | Kronix (dev-dev-delta) — Axikar gate |
| AUTH-V2-03 (MFA opcional TOTP — useMFA + ProtectedRoute guard + MfaVerify + admin-unenroll-mfa) | 2026-07-25 | **✅ PASS** | 5/5 ACs. Desvios AC2/AC3 documentados: inline check vs profile field; /settings/mfa-verify vs /mfa-verify. Bonus recovery codes + mfa_policy. admin-unenroll-mfa edge fn ✅. | Rex (dev-dev-beta) — Axikar gate |
| FIX-IG-AUT-02 (Observabilidade automações IG — agregados badge, tooltips cooldown/skip, unmatched, webhook status) | 2026-07-25 | **✅ PASS** | 5/5 ACs. useInstagramLogAggregates+useInstagramUnmatchedComments+useInstagramCommentSubscribed ✅. Badge L609-630; tooltips L467; onGoToIntegration L591; commentSubscribed aviso L540. ✅ | Serak (dev-dev-gamma) — Axikar gate |
| OBS-DISPATCH-HEALTH-01 (DB: v_dispatch_health + _get_cron_health_metrics + get_send_health RPC) | 2026-07-25 | **⚠️ CONCERNS** | AC1-3/AC7 ✅. CONCERN-1 MEDIUM: user_type 'manager' (canônico) excluído (IN usa 'gestor','admin'). CONCERN-2 LOW: SQLSTATE '02000' faltante em EXCEPTION handler. CONCERN-3 MEDIUM: _get_cron_health_metrics() sem REVOKE PUBLIC. AC4+AC5 aguardam dev-alpha. AC6 PENDING. | Bythak (dev-data-engineer) — Axikar gate |
| REL-01 (Versioned Releases — release.json + adm_releases + GH Action release-tag.yml) | 2026-07-25 | **❌ FAIL** | CRITICAL: release-tag.yml GH Action inexistente. CRITICAL: adm_releases/adm_client_versions migrations sem dev record. Story em done/ com status:backlog e Dev Agent Record vazio — não implementada. | dev-data-engineer + dev-devops — Axikar gate |
| REL-04 (Migration Discipline — lint-migrations.js + CI block + dry-run) | 2026-07-25 | **❌ FAIL** | CRITICAL: lint-migrations.yml GH Action inexistente (AC2). CRITICAL: migrations-dry-run.yml inexistente (AC3). HIGH: backfill report ausente (AC6). AC1/AC4/AC5 parcialmente implementados. | dev-devops — Axikar gate |
| FIX-SENDS-STATUS-BRIDGE-01 (Bridge delivered/read da Meta → sends_contacts via trigger SQL — P1) | 2026-07-25 | **⚠️ CONCERNS** | AC1+AC2 pre-existentes (FIX-WA-STATUS-WEBHOOK-01). AC3 ✅ trigger AFTER UPDATE messages. AC4 ✅ STATUS_RANK monotônico + error nao sobrescreve read. CONCERN-1 LOW: AC5 smoke E2E PENDING (requer prod). CONCERN-3 INFO: client-migrations.json verificar entry. | Bythak (dev-data-engineer) — Axikar gate |
| SENDS-IMPORT-01 (Simplificar importacao — remover templates e presets prontos) | 2026-07-25 | **⚠️ CONCERNS** | AC1-3/5-7/9 ✅. CONCERN-1 LOW: AC4 desvio intencional (select_matrix step antes de FileUploadZone vs "APENAS dropzone"). Espírito OK; literal AC4/AC8 nao atendidos. Lead deve confirmar desvio aceitavel. | Serak (dev-dev-gamma) — Axikar gate |
| DROP sends_import_presets (cleanup migration 20260725280000 — tabela orfã pós SENDS-IMPORT-01) | 2026-07-25 | **✅ PASS** | DROP TABLE IF EXISTS CASCADE. DO block pre-check de rows. Rollback existe. Zero callers confirmados em src/ e supabase/functions/. Aditivo seguro. | Bythak (dev-data-engineer) — Axikar gate |
| FIX-AUTH-01 (Hardening auth — fallbackProfile timeout, rate limit login, remover stubs legados) | 2026-07-25 | **✅ PASS** | 4/4 ACs; tsc EXIT 0. PROFILE_FETCH_TIMEOUT_MS configuravel+isProvisional+retry ✅. auth-login rate limit via auth_login_attempts+blocked_until+429 ✅. 3 stubs deletados ✅. | Rex (dev-dev-beta) — Axikar gate |
| FIX-SENDS-DISPATCH-02 (Retry delays reduzidos [5k,15k,45k]→[1k,3k,9k] — prevenir timeout batch) | 2026-07-25 | **✅ PASS** | 4/4 ACs. Delays [1000,3000,9000] confirmados linha 545. Max 13s/contato; 5×13=65s < 150s timeout. isRetryableError intocado. | Rex (dev-dev-beta) — Axikar gate |
| FIX-COACH-02 (Auto-trigger pos-transcricao + cron weekly_summary) | 2026-07-25 | **🔵 WAIVED** | Coach PRO completamente dropado (20260609000000): coach-evaluate, coach-email, coach_ai_settings, meeting_evaluations, playbooks — inexistentes. Todos 4 ACs inimplementaveis. Zero migration criada. | Bythak (dev-data-engineer) — Axikar gate |
| FIX-SENDS-IMPORT-06 (Input estatico lead_control reintroduzido em ImportListaTab) | 2026-07-25 | **✅ PASS** | 6/6 ACs; tsc EXIT 0. leadControlValue estado ✅. Input gateado createLeads===true ✅. Payload lead_control:trim()||null ✅. Precedencia CSV mantida (effectiveControl) ✅. Reset ✅. Toggle nao limpa estado ✅. | Serak (dev-dev-gamma) — Axikar gate |
| FIX-SENDS-IMPORT-05 (Campos personalizados visiveis no FieldMapper sem toggle createLeads) | 2026-07-25 | **✅ PASS** | 6/6 ACs; tsc EXIT 0. Gate leadExtras: (createLeads && pipelineId)→pipelineId (linha 385). AC2 pipelineId=null→[] ✅. AC4 backend sends-import-contacts:297 gate create_leads intocado ✅. AC5 autoDetectedExtras depende de createLeads independentemente ✅. | Serak (dev-dev-gamma) — Axikar gate |
| FIX-SENDS-DISPATCH-01 (Atomic claim via UPDATE+RETURNING em sends-dispatch-batch — race condition P1) | 2026-07-25 | **✅ PASS** | 5/5 ACs; tsc EXIT 0. UPDATE+RETURNING compare-and-swap atomico elimina race condition. .eq('status','running') garante invariante paused/completed nunca processados. | Rex (dev-dev-beta) — Axikar gate |
| FIX-SENDS-FILTER-02 (has_more com count real via count:exact — paginacao correta audiences grandes) | 2026-07-25 | **✅ PASS** | 4/4 ACs; tsc EXIT 0. {count:'exact'} PostgREST → totalCount via Content-Range. has_more=(offset+rawCount)<totalCount. Fallback conservador se null. | Rex (dev-dev-beta) — Axikar gate |
| FIX-SENDS-FILTER-01 (person_status ignorado em filter-leads-for-send — colisao predicado SQL P1) | 2026-07-25 | **✅ PASS** | 5/5 ACs; tsc EXIT 0. Bloco condicional .in()/.eq('active') elimina conflito AND duplo. Bug raiz: status='active' AND status IN ('inactive') sempre falso. | Rex (dev-dev-beta) — Axikar gate |
| FIX-SENDS-UI-02 (timezone UTC em scheduled_at — CriarDisparoModal.tsx toISOString()) | 2026-07-25 | **✅ PASS** | 3/3 ACs; tsc EXIT 0. new Date(localDatetime).toISOString() emite UTC sem ambiguidade Postgres. CriarDisparo.tsx ja estava correto. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-SENDS-UI-01 (started_at nao sobrescrito ao retomar disparo pausado — DisparoControls.tsx) | 2026-07-25 | **✅ PASS** | 3/3 ACs; tsc EXIT 0. isResume=send.status==='paused'. Resume: payload sem started_at. Start novo: inclui started_at. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-COACH-01 (mismatch coach_meeting_evaluations vs meeting_evaluations) | 2026-07-25 | **🔵 WAIVED** | Story supersedida: Coach PRO dropado por 20260609000000_drop_coach_pro_and_call_pro.sql. grep src/ = 0 refs ativas. Problema eliminado na raiz — nenhuma acao necessaria. | Bythak (dev-data-engineer) — Axikar gate |
| ARCH-RBAC-02 (Drop RBAC granular — migration + smart-memory; scope AC3+AC5) | 2026-07-25 | **⚠️ CONCERNS** | AC3 ✅: migration 20260725260000 completa (role_id+2 tabelas+enum+funcao, IF EXISTS, rollback). AC5 ✅: user-types-mapping.md + ADR-AUTH-04 atualizados. CONCERN-1 MEDIUM: client-migrations.json nao atualizado (falta entry 10272). AC4 pendente dev-alpha. | Bythak (dev-data-engineer) — Axikar gate |
| FIX-SENDS-CRON-LEGACY-URLS (3 crons URL legacy ohzwetkaazgxafubzvop → fn_cron_http_call) | 2026-07-25 | **⚠️ CONCERNS** | AC2 ✅ google-calendar-sync+process-meeting-followups migrados. AC3 ✅ conversion-send-retry+fn_queue_conversion_event+fn_queue_conversion_booking migrados. CONCERN-1 MEDIUM: AC6 client-migrations.json nao atualizado (falta entry 10271). | Bythak (dev-data-engineer) — Axikar gate |
| CLEAN-SENDS-01 (type safety sends_contacts — row:any removido + FK constraints migration) | 2026-07-25 | **⚠️ CONCERNS** | 4/4 ACs; tsc EXIT 0. SendContact importado; row:any removido; FK template_id+CHECK stage_ids em 20260423015000. CONCERN-1 LOW: paperwork — primeira secao ACs ainda []. | Serak (dev-dev-gamma) — Axikar gate |
| CLEAN-CRM-01 (round-robin + alias .nome→.name + useMotivosPerda TanStack) | 2026-07-25 | **⚠️ CONCERNS** | 4/4 ACs; tsc EXIT 0. useRoundRobinAssign ✅; .nome→.name em 3 arquivos ✅; useMotivosPerda useQuery+useMutation ✅; Kanban intocado ✅. CONCERN-1 LOW: paperwork — primeira secao ACs ainda []. | Serak (dev-dev-gamma) — Axikar gate |
| FWUP-18b (Storage hardening: MIME types + path-prefix INSERT policy logos — vercel buckets) | 2026-07-25 | **⚠️ CONCERNS** | AC4 CORRETO (bloqueia novos uploads SVG). CONCERN-1 MEDIUM: SVGs históricos em lp-assets ainda públicos (residual, fora escopo — sugerir FWUP-18c). CONCERN-2 MEDIUM: logos UPDATE/DELETE policies permissivas pré-existentes de FWUP-18. CONCERN-3 LOW: lp-assets INSERT/UPDATE/DELETE permissivos. CONCERN-4 LOW: fallback 'anonymous' fail-closed sem msg diferenciada. | Serak (dev-dev-gamma) — Axikar gate |
| bi-4 (Redesign enterprise BIProRevOpsTab + BIProMarketingTab — 2D funnel + MetricCell + SkeletonBlock) | 2026-07-25 | **⚠️ CONCERNS** | CONCERN-1 MEDIUM: RevOps diagnosis expand button sem aria-expanded/controls. CONCERN-2 LOW: Marketing:104 text-[10px] em legend gráfica. CONCERN-3 LOW: Edit/Save/Cancel sem aria-label. ACs visuais ✅; AC3/4/6/7 deferred por spec. | Sera (dev-dev-gamma) — Axikar gate |
| bi-3 (Redesign enterprise BIProComercialTab — SectionCard + MetricCell + ComercialSkeleton) | 2026-07-25 | **⚠️ CONCERNS** | CONCERN-1 MEDIUM: <tr onClick> expandível CloserTable sem aria-expanded/controls. CONCERN-2 LOW: caption sr-only ausente nas 3 tabelas. CONCERN-3 LOW: chevron ícone-swap vs rotate. ACs visuais ✅; AC3/5 deferred por spec. | Sera (dev-dev-gamma) — Axikar gate |
| AUTH-V2-08 (CSP connect-src: `* ws: wss:` → `'self' https: wss:` — vercel.json) | 2026-07-25 | **✅ PASS** | CONCERN-1 LOW: checkboxes ACs não marcados no arquivo (paperwork). Remoção de `*` e `ws:` validada: grep src/ confirma zero fetch() http:// e zero ws:// conexões. OAuth+Supabase+Gemini WSS preservados. | Serak (dev-dev-gamma) — Axikar gate |
| ALTIORA-04 (terminologia "Negócio"→"Referral" no pipeline Altiora — pipelineLabels.ts + 6 arquivos) | 2026-07-25 | **✅ PASS** | 5/5 ACs; tsc EXIT 0; pipelineLabels.ts clean; no-explicit-any/no-case-declarations pre-existentes de stories anteriores (não regressão). | Nova (dev-dev-alpha) — Axikar gate |
| ALTIORA-V1 (stories 01–25 — quality gate completo, rodada 2 final) | 2026-07-25 | **CONCERNS** | CRITICAL resolvido entre rodadas: `useAltioraStageTransition.ts` — 6 `.eq('lead_id')` em queries `meetings` corrigidos para `.eq('leads_id')` (lines 77,92,121,136,151,166). `tsc --noEmit` EXIT 0; `npm run build` 7.67s. Sem regressoes. **[CONCERN-1 MEDIUM] altiora-email-referral-inbound/index.ts:129,130,139,140,141,146** — 7 `no-useless-escape` em regex (hifens em `[]`). Runtime correto; falha eslint. **[CONCERN-2 LOW] NegociosSection.tsx (conversas):159,160,252,375,376,380,436,572-574** — 14 `no-explicit-any` pre-existentes (nao regressao ALTIORA). Aprovado com observacoes. Pronto para push. | devs responsaveis — Axikar gate |
| KFY-4.1 (registro manual de token de webhook + FIX CRÍTICO client_id≠account_id — kiwify-connect + migration `20260702170000`) | 2026-07-02 | **PASS** | 7/7 ACs; verificação final pré-push do épico. `deno check` limpo em kiwify-connect + kiwify-client; `tsc` verde (frontend 3 campos + seção manual). **(a)** Migration no LIVE: `client_id text NOT NULL`, status CHECK com `pending_webhook`, 0 rows, registrada — coerente com código. **(b)** Ações antigas intactas (status/disconnect/list_products/reprocess); test/connect passaram a exigir `client_id` — mudança de contrato INTENCIONAL (fix OAuth AC7), frontend em lockstep. **(c)** Secrets OK — client_secret/webhook_token nunca retornados/logados; client_id é identificador plaintext (não secret). **(d)** Fluxo manual: `save_credentials` retorna `?cid=<row.id>` que bate com `.eq('id',cid)` do `resolveConnection` (resolve mesmo em pending_webhook); `register_manual_webhook_token` criptografa token→connected. **CONCERN-1 (MEDIUM, PRÉ-EXISTENTE KFY-1.6, não-bloqueante):** `kiwify-reconcile` falha `deno check` (SupabaseClient bare `@supabase/supabase-js` vs esm.sh no `_shared`, TS2345 :96) — confirmado idêntico no parent de `99d5a97`; type-only (Deno apaga tipos, cron roda); story só adicionou client_id ao select. Recomendo unificar o import specifier. **Nota deploy:** functions+frontend juntos (body exige client_id; 0 rows, sem regressão). **Push LIBERADO.** | Serak (dev-dev-gamma) — Axikar gate |
| KFY-2.3 (badge de curso no lead kanban/mobile/detalhe + fallback manual de produto — frontend, não God Node) | 2026-07-02 | **CONCERNS** | 7/7 ACs. `tsc --noEmit` zero erros nos 7 arquivos tocados; `npm run build` verde (dev). **Smoke do caminho de dados no LIVE:** `authenticated`+`anon` têm GRANT SELECT em `kiwify_lead_products` (igual à event_mappings que já funciona), RLS SELECT p/ user ativo, 1 FK people_id→clients_people → embed PostgREST `cursos:kiwify_lead_products(...)` resolve. `CursoBadges.tsx` reusa EXATAMENTE a classe de chip de StageColumn (amber, distinta de verde WA/violeta IA), `GraduationCap`, null sem cursos (não quebra), truncate + `+N`; renderizado em kanban/mobile/sidebar. Fallback manual: `useManualProducts`(localStorage)+`mergeProducts`+`ManualProductEntry` (classes MetaIntegrationConfig), ambos dialogs religados. **CONCERN-1 (MEDIUM, débito, não-bloqueante):** nome do produto manual só em localStorage (per-browser); o `product_id` (usado pelo routing do worker) É persistido no DB, então funciona tenant-wide — só o NOME degrada p/ id cru em outro browser/usuário. Aceito (story proibiu tabela nova, dev documentou) → recomendo story futura de persistência tenant-wide do nome. **Obs LOW:** smoke visual em browser não executado (sem sessão) — recomendo 1 pós-deploy. **Push LIBERADO.** | Novik (dev-dev-alpha) — Axikar gate |
| KFY-2.2 (`kiwify_lead_products` M-N curso↔contato + upsert de posse em `kiwify-process-event`, God Node) | 2026-07-02 | **PASS** | 7/7 ACs. `deno test` 45/45 ✅. Schema LIVE bate 100% com spec (id, people_id→clients_people CASCADE, product_id/product_name NOT NULL, connection_id→kiwify_connections SET NULL, UNIQUE(people_id,product_id), índice people_id, migration `20260702160000` registrada). RLS `relrowsecurity=true`, write=`(super_admin OR user_type='manager')`. **AC3** upsert gated por `isPossessionTrigger` (só compra_aprovada/subscription_renewed) + productId+productName; usa person.id canônico + event.connection_id; `first_seen_at` omitido → intocado no conflito. **AC5** bloco em `:290` **após** guard de precedência (early-return ignored `:251`) → evento ignored não popula; idempotente via onConflict. **AC6** upsert em try/catch (log.error, não derruba o evento — enriquecimento). **AC4** zero DELETE → reembolso/chargeback/cancel não removem. Mudança aditiva/isolada, sem regressão no God Node. **Obs LOW:** commit `3c26f83` empacota também mudanças KFY-2.1 (boletoExpiry/vencimento_boleto) — higiene de escopo; compra_aprovada sem product_name pula badge (aceitável). **Push LIBERADO.** | data-engineer (migration+RLS) + dev-dev-beta-2/Rexar (edge AC3) — Axikar gate |
| KFY-1.8 (QA gate God Node do épico `kiwify-integracao` — Fase 1) — **rodada 2 (final)** | 2026-07-02 | **PASS** | Os 2 CRITICAL da rodada 1 corrigidos e revalidados na fonte de verdade. **CRITICAL-1:** migration `20260702140000` registrada no LIVE — `messages_source_type_check` agora inclui `'kiwify'` (aditivo, 859 rows intactas) → INSERT do worker passa. **CRITICAL-2:** commit `9f64508` — `KiwifyIntegrationConfig.tsx:880` grava `t.nome`; `useWhatsappTemplates.ts:47` mapeia `nome:item.name` → valor = coluna DB `name`, casa com `.eq('name',...)` do worker e `template_name` de whatsapp-outbound. `deno test` 43/43 ✅. CONCERNS não-bloqueantes permanecem (enforce_signature=false, gate opt-in no worker, orquestração God Node sem teste executável, cancelJobs cross-produto, RLS write omite admin não-super_admin). **Push/deploy LIBERADO.** | KFY-1.5 (data-engineer) + KFY-1.7 (dev-alpha) — Axikar gate |
| KFY-1.8 (rodada 1 — FAIL, superada) | 2026-07-02 | **FAIL** | **2 CRITICAL no caminho de envio (God Node `messages`), latentes até configurar template real.** `deno test` 43/43 ✅; migrations `20260702120000`+`20260702130000` aplicadas no LIVE; seeds 10 mappings/4 automations/4 stages/optin OK. **[CRITICAL-1]** `kiwify-dispatch-worker:171` insere `messages` com `source_type='kiwify'` → viola `messages_source_type_check` (LIVE: inbound/manual/ai_agent/campaign/form/followup/appointment_reminder) → todo INSERT lança → job `failed`, nada envia. Fix: migration incluindo `'kiwify'` no CHECK (data-engineer/KFY-1.5). **[CRITICAL-2]** `KiwifyIntegrationConfig.tsx:880` grava `template_id=t.id_template` (id Meta numérico) mas o worker resolve por `.eq('name',...)` e envia `template_name` (=coluna `name`, contrato `whatsapp-outbound:55`) → lookup+envio Meta falham ao configurar automação. Fix: UI gravar `t.name` (KFY-1.7). **PASS:** AC1 mapping (FKs LIVE OK, fallback produto→default), AC2 precedência, AC3 idempotência (UNIQUE composta), AC4 cancelamento, AC6 opt-in (canSend), AC7 secrets (zero vazamento; só `*_enc` via app_encrypt_secret), RLS fix `gestor`→`manager` correto vs domínio LIVE, merge kiwify-connect KFY-1.3+1.7 sem conflito. **CONCERNS:** enforce_signature=false (injeção não-autenticada até validar webhook real — ratificado), gate opt-in no worker vs arquitetura §2.2, orquestração God Node sem teste executável, cancelJobs cross-produto (LOW), RLS write omite `admin` não-super_admin (LOW, inócuo hoje). | KFY-1.1..1.7 (devs) — Axikar gate |
| TIKTOK-05 (QA gate final integração TikTok DM — migration constraint + oauth-callback fn + UI prefill) | 2026-06-23 | **PASS** | 7/7 ACs verdes. `npx tsc --noEmit` exit 0. **AC1** `tiktok-oauth-callback` responde 405 (≠404, deployada; POST handler + OPTIONS 204). **AC2** constraint `omni_channel_configs_channel_check` (banco LIVE `wotuyxscsfralqpoiyfv`) inclui `'tiktok'` no ARRAY. **AC3+AC6** wiring confirmada e SEM regressão — `IntegracoesConfig.tsx:128` mapeia tab `tiktok`→`TikTokIntegrationConfig` (wrapper sub-tabs DMs/Ads, default `dms`)→`TikTokMegaConfig` (lazy `:5,83`); MegaConfig usa `useSearchParams` (não `window.location.search`), trata `connected=1`/`error` com toast + cleanup via `setSearchParams replace`. **AC4** App ID default `sbawma1uwbrucrpyd7` (`DEFAULT_APP_ID`) prefill nos 2 useEffects + placeholder. **AC5** `TiktokCallback.tsx` compila; rota `/tiktok/callback` registrada em `App.tsx:148`; sucesso navega `DEFAULT_REDIRECT + '&connected=1'` (`:87`). **AC7** `tiktok-inbound` 200 + `tiktok-outbound` 500 (≠404, deployada — 500 é env/payload, fora de escopo). **Segurança:** ZERO secret hardcodado no frontend (scan em MegaConfig+Callback); edge fn lê `TIKTOK_APP_ID`/`TIKTOK_APP_SECRET` via `Deno.env.get` (`:139-140`), `app_secret` NÃO entra no JSON `credentials` salvo (`:181-190` — só app_id/tokens/open_id), retorno estruturado `{success,...}`, CORS+OPTIONS OK. **CONCERN-1 (LOW, não-bloqueante):** `TiktokCallback.tsx:47` valida CSRF state só quando `state && savedState` ambos presentes; se `savedState` ausente (sessionStorage limpo/cross-device) a checagem é pulada (degradação suave). Aceitável — TikTok exige redirect_uri exato registrado, mitigando. **NOTA:** fluxo OAuth e2e + webhook NÃO testados (OUT de escopo, requer conta TikTok real) — débito de smoke manual pós-conexão real (`feedback_runtime_smoke_before_gate.md`). Git HEAD em `21d772c` (commit TIKTOK-04). Push LIBERADO. | data-engineer (T-01) + beta (T-02) + alpha (T-03) + devops (T-04) — Axikar gate |
| FIX-AGENT-DIAGNOSTICO-01 (drafts v5 do agente Diagnóstico — identity / general_rules / step_fluxo_completo) | 2026-06-16 | **PASS** (final) | Gate DESIGN read-only dos 3 drafts em `docs/smart-memory/agents/diagnostico-agent-v5/`. **8/8 ACs atendidos, ZERO observações bloqueantes ou não-bloqueantes.** Gate re-rodado após ajustes de key do dev-architect (`feedback_git_state_freshness.md` aplicada: re-li os 3 drafts no estado atual antes de fechar). Ponto de escopo em aberto RESOLVIDO: `diagnostico-agent-v5-field-validation.md:88-111` confirma via banco LIVE (`wotuyxscsfralqpoiyfv`) que `interesse_area`, `perfil_tipo`, `objetivo`, `momento` são todos custom fields GLOBAIS (`pipeline_id=NULL`, `entity_type=pessoa`, ativos) → saves via `atualizar_pessoa` no pipeline Consultoria persistem. **Verificações críticas (10/10):** (1) `q22_close_probability` → ZERO (RF-1, AC3); (2) `perfil_tipo` byte-exato `empresário`/`profissional`/`freelancer`/`estudante` COM acento — único `empresario` sem acento é instrução negativa ("NÃO mande `empresario`"), RF-2, AC2; (3) `conversation_summary` capturado ao final + antes de rotear, ∈ ALLOWED_Q_COLS hardcoded (index.ts:1604), RF-4, AC3; (4) `8 encontros`/`8 semanas` → ZERO; "7 semanas, 7 encontros" em identity.md:21-24, RF-5, AC4; (5) Stage IDs exatos `72257dda`/`64b555af`/`2a83ef9f` validados contra live-state.md:186-192, AC6; (6) `temperature` solto → ZERO; só `pre_sale_temperature`, RF-6; (7) keys `salvar_qualificacao` todas ∈ ALLOWED_Q_COLS (index.ts:1596-1605) — **drafts usam `goal`/`moment` (keys do executor que populam `ctx.objetivo`/`ctx.momento` em index.ts:1624-1625) e NÃO as duplicatas pt-BR**; `step:27` adiciona regra explícita reforçando; `objetivo`/`momento` restantes são placeholders `{{...}}` de display, não keys; (8) `atualizar_pessoa` só `name`/`email`/`notes` (STATIC_PESSOA_FIELDS) + custom globais `interesse_area`/`perfil_tipo`; (9) `bloquear_ia` em escalation e EXCLUÍDO de agendamento; (10) `q6_trigger` text livre grava sem rejeição. **Teto de tools (re-verificado no estado atual):** todos os 4 paths de ação respeitam 4 tools/turno — FIT CONSULTORIA agora tem exatamente 4 (`q19`+`atualizar_etapa`+`atualizar_lead`+`enviar_link_agendamento`), com `q6_trigger`/`q21`/`conversation_summary` movidos pro turno seguinte. **O CONCERN-1 LOW da rodada inicial (FIT CONSULTORIA listava 5 tools sob cabeçalho "até 4") foi RESOLVIDO pelo dev-architect.** Drafts são design specs — apply LIVE (v4→v5) pendente pelo dev-data-engineer via Safety Protocol. Push/apply LIBERADO sem ressalvas. | dev-architect (drafts) — Axikar gate |
| FIX-OMNI-REFRESH-REMOVEQUERIES (botão refresh `/omni` troca `refetch()` por `queryClient.removeQueries`) | 2026-06-16 | **CONCERNS** | Iteração 3 do refresh Omni. Diff em `OmniMensagens.tsx`: importa `useQueryClient`; remove `refetch` da desestruturação do hook (modo omni), mantém `dlEntries.refetch()` no dead-letter; botão omni agora `onClick={() => queryClient.removeQueries({ queryKey: ['omni-mensagens'] })}`; spinner/disabled via `isLoading || isRefetching`. **Funciona, sem bug bloqueante.** Verificado: (1) `removeQueries` com observer ativo na tela limpa o cache e dispara re-fetch automático (RQ v5 re-subscreve sem dados → `isLoading=true` → busca do zero), replicando F5; (2) SEM colisão de queryKey — grep confirma que só `useOmniMensagens.ts:38` usa o prefixo `['omni-mensagens', filters]`, então o fuzzy-match remove apenas essa família (todas as variações de filtro), comportamento desejado; (3) filtros e busca por texto respeitados pós-remove, pois a query re-monta com `filters` correntes no key; (4) empty-state NÃO pisca — `OmniMensagens.tsx:428` gateia "Nenhuma mensagem encontrada" com `!isLoading && rows.length===0`, e L422 mostra "Carregando…" durante `isLoading`; (5) dead-letter inalterado (`refetch()`); (6) `tsc --noEmit` exit 0; nenhum `refetch` órfão (único restante é `dlEntries.refetch()` L278). **CONCERN-1 (LOW — regressão de UX, intencional):** `removeQueries` descarta TODAS as páginas paginadas; quem rolou N páginas volta ao topo com 1 página após o refresh. É o comportamento de F5 (o que o usuário pediu), mas é regressão real vs o `refetch()` da iteração anterior que preservava o scroll. Aceito por alinhamento ao pedido "igual F5". **CONCERN-2 (INFO — redundância/diagnóstico impreciso):** a justificativa do dev ("refetch reusa referências via structural sharing e não re-renderiza") é tecnicamente imprecisa — structural sharing só preserva referência quando os dados são IDÊNTICOS; chegando mensagem nova, a referência muda e o re-render ocorre. O fix anterior (ordenação `sent_at`→`created_at`, que segue no hook intocado) JÁ resolvia a causa-raiz; esta iteração é mais agressiva que o necessário. Não é bug, mas `removeQueries` + ordenação `created_at` são duas soluções para o mesmo problema. **NOTA:** smoke visual do roteiro do usuário (deletar contato + WhatsApp real + refresh) não executável por mim (sem browser/sessão logada neste ambiente); recomendado 1 smoke manual confirmando filtros/busca/scroll-reset. Push LIBERADO com observações. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-OMNI-REFRESH-FUNCIONAL (ordenação `useOmniMensagens` `sent_at`→`created_at` — root cause REAL do refresh) | 2026-06-16 | **PASS** | **Causa-raiz provada na fonte de verdade.** O fix visual anterior (linha abaixo) tratava só o sintoma; o bug funcional era a ORDENAÇÃO. Diff em `useOmniMensagens.ts`: `.order('sent_at', {nullsFirst:false}).order('created_at',...)` → `.order('created_at', {ascending:false})`; cursor keyset `lt('sent_at')` → `lt('created_at')`; `nextCursor` `lastItem.sent_at` → `lastItem.created_at`; filtros `dateFrom/dateTo` `sent_at` → `created_at`; display `ts = sent_at ?? created_at` PRESERVADO. **Premissa verificada contra o banco/edge fns (não só grep):** (1) `messages.created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()` — migration `20250624200458:75` (o `created_at: string | null` em types.ts é conservadorismo do gerador Supabase, não o DDL real) → keyset `lt('created_at', cursor)` é monotônico e NUNCA exclui linhas por NULL; (2) `whatsapp-inbound/index.ts:973-991` INSERT em messages NÃO inclui `sent_at` → mensagens recebidas entram com `sent_at=NULL`; (3) `whatsapp-outbound/index.ts:1007,1058` seta `sent_at` no UPDATE pós-envio → `ts = sent_at ?? created_at` continua exibindo horário de envio correto no outbound. **Mecânica do bug confirmada:** ordenar por `sent_at DESC nullsFirst:false` empurrava toda conversa inbound (sent_at NULL) pro fim das páginas; o `refetch()` recarrega só a 1ª página (cronologicamente "topo"), que nunca continha a mensagem nova → só aparecia após F5. Ordenar por `created_at` (sempre presente, populado no momento exato da gravação) põe a mensagem nova no topo da 1ª página. **Regressões avaliadas:** infinite scroll OK (keyset coerente com a nova ordenação, cursor sempre presente); display de horário OK; único consumidor é `OmniMensagens.tsx` (grep confirma); `tsc --noEmit` exit 0. **CONCERN-INFO (não-bloqueante):** filtros `dateFrom/dateTo` agora filtram por `created_at` em vez de `sent_at` — mudança semântica TEÓRICA apenas, pois a UI (`OmniMensagens.tsx:180-182`) nunca popula esses campos (só channel/direction/source_type/search); dead-path no consumidor atual. **NOTA QA:** o smoke runtime visual do roteiro do usuário (deletar contato + WhatsApp real + clicar refresh) NÃO foi executado por mim (sem browser/sessão logada neste ambiente), MAS desta vez a causa-raiz está PROVADA no schema + ambos webhooks — confiança alta, diferente do Round 1 cuja tese não foi validada contra o comportamento dos dados. `feedback_runtime_smoke_before_gate.md` aplicada de forma mais forte: tracei o dado real (NOT NULL/NULL nas duas pontas) em vez de só o caminho React. Push LIBERADO. | Novik (dev-dev-alpha) — Axikar gate |
| FIX-OMNI-REFRESH-VISUAL (botão refresh `/omni` — `isRefetching` + `animate-spin`) — **SUPERSEDED pelo fix funcional acima; diagnóstico "só feedback visual" estava incorreto** | 2026-06-15 | **CONCERNS** | Fix correto e bem direcionado. Diff em `OmniMensagens.tsx`: L184 desestrutura `isRefetching`; botão refresh (L270-278) ganha `disabled={showDeadLetter ? dlEntries.isRefetching : isRefetching}` e `RefreshCw className={... ? 'animate-spin' : ''}`. **Análise runtime (caminho completo traçado):** clique→`refetch()`/`dlEntries.refetch()` (ramo por `showDeadLetter`)→`refetch` de `useInfiniteQuery` refaz a 1ª página com `pageParam=null` (comportamento padrão TanStack, traz mensagens novas no topo)→`data` muda referência→`rows = useMemo(..., [data])` (L190) recomputa→re-render. Todas as etapas conceitualmente corretas. `isRefetching` é a flag certa (exclui `isFetchingNextPage`), logo **sem regressão no infinite scroll** — ícone não gira durante paginação. Sem toque em mutações (notas/reuniões/retry dead-letter) nem na sentinela de scroll. Root cause do dev (só faltava feedback visual; `refetch()` sempre funcionou) é coerente com `refetchInterval: 30_000` já existente no hook. Validações: `tsc --noEmit` exit 0; `isRefetching` confirmado como export válido do query-core; dev server (5173) responde `/omni` HTTP 200 com HMR. **CONCERN-1 (INFO — não-bloqueante):** o passo "nova mensagem aparece no topo sem reload" do roteiro NÃO foi verificado por mim em runtime visual (sem browser interativo / sessão logada neste ambiente). É correto por design mas não observado empiricamente — recomenda-se o smoke manual descrito pelo dev antes do push. Aplica `feedback_runtime_smoke_before_gate.md`: o gate anterior FIX-POLLING-REFRESH-01 passou só com grep+tsc e o bug persistiu, então aqui tracei o comportamento React Query etapa a etapa em vez de só validar tipos. | Novik (dev-dev-alpha) — Axikar gate |
| CAL-COM (CAL-DB + CAL-BE + CAL-LINK + CAL-FE — integração Cal.com OAuth PKCE) | 2026-06-13 | **CONCERNS** | 11/12 ACs PASS; 1 AC (AC12 RLS) com defeito não-bloqueante. `tsc --noEmit` exit 0; eslint dos 4 arquivos FE exit 0. **PASS:** AC1 `calcom-connect` PKCE puro sem `client_secret` (`index.ts:79-85`); AC2 refresh `.eq('id', conn.id)` (`calcom-client.ts:115`); AC3 HMAC-SHA256 via `x-cal-signature-256` (`calcom-webhook.ts:122-127`); AC4 sempre 200 exceto HMAC mismatch (catch externo `:206` retorna 200); AC5 disconnect DELETE webhook antes da row (`calcom-disconnect.ts:64-82`); AC6 challenge S256 via `crypto.subtle.digest('SHA-256')` + `code_challenge_method=S256` (`CalendarSyncCard.tsx:67-91`); AC7 CSRF state via sessionStorage validado antes da edge fn (`CalcomOAuthCallback.tsx:38-44`); AC8 `lead_responsavel_id` usado (`ai-agent-execute.ts:1830-1835`); AC9 fallback `/agendar/${leadId}?d=30` idêntico ao prévio (`:1825,1850`); AC10 tsc clean; AC11 migration aditiva (todos `IF NOT EXISTS`/`ADD COLUMN IF NOT EXISTS`, `UPDATE ... WHERE calcom_client_id IS NULL`). Segurança: tokens não retornados nas respostas (só `username`+`default_booking_url`); `webhook_secret` nunca exposto via API pública (hook FE seleciona colunas explícitas sem secret/tokens — `useCalcomConnection.ts:31`); `calcom-connect`/`disconnect` validam JWT antes de qualquer op. **CONCERN-1 (HIGH, não-bloqueante — defeito de RLS em AC12):** policy `gestores_see_all_calcom` usa `user_type IN ('super_admin', 'gestor')`, mas `super_admin` é coluna BOOLEAN em `settings_users` (migration 20251110183840:28), NÃO um valor de `user_type` (valores válidos: gestor/consultor/atendente/cliente/gerente — migration 20260126175751). Consequência: (a) branch `super_admin` é dead code; (b) um super_admin que NÃO seja também `gestor` recebe DENY no SELECT de conexões de terceiros. Convenção do projeto (≥15 policies, ex. callpro_rls_fix) é `super_admin = true OR user_type = 'gestor'`. RLS de isolamento básico (users_own) está correto — usuário comum não vê conexões alheias. Corrigir para `(super_admin = true OR user_type = 'gestor')`. **CONCERN-2 (LOW, não-bloqueante):** webhook resolve a connection (e portanto o `webhook_secret` da verificação HMAC) a partir de `organizerEmail`/`metadata` do payload NÃO verificado; é o padrão Cal.com (secret per-connection), mas se `conn.webhook_secret` for NULL o handler aceita sem HMAC (`:128-130`) — aceitável pois connect sempre grava secret. **CONCERN-3 (INFO):** migration aplicada ao banco não é verificável estaticamente; coordenar apply com Grav. | data-engineer (CAL-DB) + beta (CAL-BE) + gamma (CAL-LINK) + alpha (CAL-FE) — Axikar gate |
| WAT-SYNC-01 + WAT-SYNC-02 + WAT-SYNC-03 (fix sync match-by-name + auto-sync cron + testes Deno — commit `8653733`) | 2026-06-10 | **CONCERNS** | 16/17 ACs atendidos; 1 AC (WAT-SYNC-02 AC1) bloqueado por design/setup manual aceito pelo lead. **PASS** verificado: (1) 3 blocos de match `id_template`→`slug`→`name` em `_shared/template-sync-lib.ts:169-186`; (2) `shouldSoftDelete` pula `pending_*` (`:137-141`) — confirmado ao vivo que existem **2 templates `pending_*`** no banco (cenário real protegido); (3) log `id_template_fixed` emitido só quando `previousIdTemplate` é `pending_*` (`index.ts:182-188`); (4) `x-cron-key`/`Authorization Bearer` comparados contra `SUPABASE_SERVICE_ROLE_KEY` env, **nunca hardcoded**, com guard `serviceRoleKey.length > 0` que impede bypass quando env vazia (`index.ts:58-60`); (5) `secure_http_post` lê secret do Vault (`vault.decrypted_secrets`), SECURITY DEFINER + REVOKE PUBLIC / GRANT service_role (migration `20260422001300`); (6) migration cron idempotente (unschedule→schedule) com skip gracioso + NOTICE quando Vault secret ausente (`20260610000003:24-54`). **Verificação ao vivo:** `last_synced_at` existe (count=1), cron job `whatsapp_templates_auto_sync` ausente (count=0 — esperado, Vault secret `service_role_cron` ausente count=0), `secure_http_post` existe (count=1). **Testes:** `deno test` 11 passed / 0 failed; `deno check` exit 0; contrato de resposta `{synced,created,updated,submitted,deleted}` intacto vs `WhatsappTemplatesConfig.tsx:109`. **CONCERN-1 (INFO — setup manual aceito pelo lead, não bloqueante):** cron job NÃO está agendado até criar o Vault secret `service_role_cron` no Dashboard + re-rodar a migration idempotente; até lá o auto-sync de 5min não roda (sync manual via UI funciona). Documentado em `manual_setup_pending` da story WAT-SYNC-02. **CONCERN-2 (LOW — não-bloqueante):** `deno lint` reporta `no-import-prefix` nos imports `https://` — convenção pré-existente das 82 edge functions, `deno lint` não roda no CI; sem impacto. | Rex (WAT-SYNC-01/02) + Kron (WAT-SYNC-03) — Axikar gate |
| FIX-IG-DM-TOKEN-REFRESH (Fix 1 `instagram-oauth` salva `app_id` + Fix 2 `instagram-token-refresh` fallback `INSTAGRAM_APP_ID`) | 2026-06-10 | **CONCERNS** | Os 2 fixes relatados estão corretos e presentes em disco (git diff HEAD confirma ambos os diffs). **PASS** em: (1) `instagram-oauth/index.ts:192` salva `app_id: appId` no credentials do upsert; (2) `instagram-token-refresh/index.ts:85` fallback `credentials.app_id \|\| Deno.env.get('INSTAGRAM_APP_ID') \|\| ''`; (3) campo lido bate com campo salvo (`app_id`); (4) backward-compat resolvido sem re-OAuth **desde que** env `INSTAGRAM_APP_ID` esteja setada no projeto Supabase — guard L100-110 já alerta via `omni_channel_alerts` se faltar `app_id`/`app_secret`. **CONCERN-1 (HIGH — segundo path de credenciais ignorado, não regressão):** `meta-save-credentials/index.ts:242-248` persiste um **System User Token** (EAA perpétuo, SEM `token_expires_at`) no MESMO `omni_channel_configs(channel='instagram')`. O refresh trata `token_expires_at` ausente como `daysUntilExpiry = -1` (L117) → **força `fb_exchange_token` em TODA execução diária** sobre um token que não precisa/deve ser trocado. O fix não distingue esse cenário. **CONCERN-2 (HIGH — causa raiz pode ser de tipo de token):** `instagram-outbound/index.ts:79-82` distingue **IGA tokens** (`IGAF`/`IGQVJx`/`IGQVJt`, via `graph.instagram.com`) de EAA. `fb_exchange_token` em `graph.facebook.com` só renova **EAA/Facebook Login OAuth**. Se o token for IGA, o refresh falha (domínio errado). O refresh assume EAA universalmente — sem detecção de tipo. **CONCERN-3 (MEDIUM — edge case prompt #4):** token JÁ expirado **não** é recuperável por `fb_exchange_token` (Meta exige token vivo para a troca). O fix previne expiração futura (folga ≥7d via `REFRESH_THRESHOLD_DAYS`) mas **não recupera o token morto** que causou o OAuthException 190 reportado — esse exige re-autenticação OAuth manual. | (fix não atribuído a dev — Axikar gate) |
| LOSS-01 + LOSS-02 + LOSS-03 (feature motivos de perda — migration seed + ação "Marcar como Perdido" kanban/lista + filtro por motivo na toolbar) | 2026-06-10 | CONCERNS | 18/18 ACs atendidos. `tsc --noEmit` exit 0; `npm run build` exit 0 (6.79s). **LOSS-01** (5/5): migration `20260610000001_seed_default_loss_reasons.sql` com 5 INSERTs UUIDs fixos (`a1b2c3d4-0001-…-0001..0005`) + `ON CONFLICT (id) DO NOTHING` idempotente, todos `active=true`. **LOSS-02** (6/6): `StageColumn.tsx` + `NegociosList.tsx` com DropdownMenuItem "Marcar como Perdido" gated `status==='in_progress'`, `MotivoPerdasModal` + `handleConfirmLost` chamando `useUpdateNegocio.mutateAsync({status:'lost', leads_loss_reasons_id, loss_reason})` — colunas confirmadas no schema `leads` (types.ts:3094/3098 + FK `leads_leads_loss_reasons_id_fkey`). **LOSS-03** (7/7): `NegociosToolbar` Select gated `statusFilter==='perdido'` (valor PT) + "Todos os motivos"; `useNegociosOptimized.ts` (hook real, não useNegocios.ts) aplica `.eq('leads_loss_reasons_id', motivoFilter)`; reset em `handleStatusFilterChange` + `clearSecondaryFilters`. **Bug fix pré-existente validado**: `statusDbMap` (perdido→lost/ganho→won/em-andamento→in_progress) cobre 100% dos 5 valores do Select (+ branches `sem-perdidos`/`todos`). Segurança: zero `dangerouslySetInnerHTML` novo, `motivoFilter` parametrizado via supabase client. **CONCERN-1 (LOW, pré-existente, não-regressão)**: `KanbanBoard.tsx:100-109` monta queryKey otimista de drag omitindo `motivoFilter`+utm+score — mismatch com `useNegociosByStage` (objeto completo); só afeta cache otimista do drag, corrigido pelo realtime invalidation. **CONCERN-2 (INFO)**: `useNegocios.ts:201-202` (hook legado, path NÃO usado por LOSS) referencia `motivo_perda`/`motivo_perda_id` (tabela `crm_motivo_perda` legada) — divergência nominal sem impacto no fluxo LOSS. Migration aplicada ao banco não verificável estaticamente (AC4 LOSS-01 — confiar no relato do data-engineer). | data-engineer (LOSS-01) + alpha (LOSS-02 + LOSS-03) — Axikar gate |
| CRM-NOTES-01 | 2026-06-09 | PASS | nenhum bloqueante. AC1-AC5 verificados: 0 Textarea puro em notes da pessoa, 6 pontos de edição usam RichTextEditor, ambas ocorrências de `dangerouslySetInnerHTML` em notes sanitizadas via DOMPurify, `tsc --noEmit` exit 0, `npm run build` exit 0 (8.28s). LOW (não-bloqueante): EditarPessoaModal só alcançável no MobileClientesTabs; no desktop "Editar" navega para ClienteSingle — ambos usam RichTextEditor, sem gap. | Novik (dev-dev-alpha) |
| SQUAD-C-DB-AUDIT | 2026-06-08 | FAIL→PASS (v2) | v1: 7 CRITICAL + 3 HIGH; v2: PASS condicional, 3 LOW (llm_connections binarização, pg_trigger query, canônica usuários); aguarda squad-c-prod-validations.md + PO/Squad-B acks | squad-c-architect / squad-c-data-1 / squad-c-analyst |
| AUTH-V2-03c  | 2026-04-26 | CONCERNS  | 3 LOW originais + HIGH adicional em re-review (atomicidade recovery flow) | Nova (dev-dev-alpha) |
| BI-VOICE-00  | 2026-04-26 | CONCERNS  | MEDIUM: RLS row-level deixa api_key acessível a gestor via SELECT direto (pré-existente) | byte (dev-data-engineer) + dev-dev-alpha |
| BI-VOICE-01  | 2026-04-26 | CONCERNS  | 5 non-bloqueantes (race condition rate limit, provider_error echo, key na URL, rate limit sem user_id, retry_after fixo) | rex (dev-dev-beta) |
| BI-VOICE-02  | 2026-04-26 | CONCERNS  | 3 HIGH (error codes contrato, dead state thinking, reconnectRef órfão) + 2 MED + 2 LOW | Nova (dev-dev-alpha) |
| AUTH-V2-01 (PR #36) | 2026-04-26 | PASS | 2 LOW (UX dois spinners; AC5 não verificável estaticamente) | rex (dev-dev-beta) |
| BI-VOICE-02-fixup (c4908b57) | 2026-04-26 | PASS | nenhum bloqueante (3 issues prévias remanescentes — blob URL global, sem unit tests, AC3 text desatualizado) | Nova (dev-dev-alpha) |
| AUTH-V2-03c-fixup | 2026-04-26 | PASS | 1 LOW (comentário useMFA.ts:87 impreciso sobre AAL1 vs AAL2 unenroll) | byte (dev-data-engineer) |
| BI-VOICE-04 (PR #39) | 2026-04-26 | PASS | 3 LOW (AC2/AC4/AC7 smoke staging-only; audit log do dialog não verificado; settings_users.active dependency) | Nova (dev-dev-alpha) |
| FIX-IG-AUT-01 | 2026-04-30 | CONCERNS | CONCERN-OPS: tenants pre-existentes precisam re-subscribe manual de `comments`; CONCERN-TEST: sem cobertura automatizada do fluxo comment→runner | Rex (dev-dev-beta) |
| SENDS-PRO-REVISAO (IMPORT-01 + IMPORT-02 + 9 fixes) | 2026-04-30 | PASS | Débito declarado: P1-03 (atomic CAS batch), P2-07 (leads existing people). Cosméticos não-bloqueantes: `any` em arquivos do escopo, commit 39b0d013 com mensagem incompleta | gamma (dev-dev-gamma) + delta (dev-dev-delta) |
| SENDS-IMPORT-02 (re-verificação) | 2026-04-30 | CONCERNS | AC8 parcial: tipo `field_mapping.company?` mantido em ImportContactsInput por design defensivo (warning + duck-typing); LOW: rótulo trade_name="Nome Fantasia" vs "Nome da empresa"; LOW: 'documento' alias colide entre cpf e tax_id; AC11 smoke test não evidenciado | gamma (dev-dev-gamma) |
| FIX-SENDS-01 | 2026-04-30 | CONCERNS | LOW: migration 20260423013000 é DUPLICATA EXATA da 20260423010000 (idempotente; remover em cleanup); LOW: sends-dispatch-batch sem entrada explícita em config.toml; AC4 smoke test não evidenciado | gamma (dev-dev-gamma) |
| FIX-SENDS-IMPORT-05 | 2026-04-30 | PASS | nenhum | gamma (dev-dev-gamma) |
| FIX-SENDS-IMPORT-06 | 2026-04-30 | PASS | nenhum | gamma (dev-dev-gamma) |
| FIX-IG-OUTBOUND-IGA-ROUTING (commit 987057b3) | 2026-04-30 | PASS | nenhum | rex (dev-dev-beta) |
| FIX-OMNI-DIRECTQUERY-IG-TIKTOK (commit 857a6b0e) | 2026-04-30 | CONCERNS | LOW pré-existente (não regressão): filtroCanais.size < 5 em Conversas.tsx:670 desatualizado vs >= 6 em :406 desde commit 9777202c | Nova (dev-dev-alpha) |
| FIX-IG-AUTOMATION-IDEMPOTENCY (commit 3524ce71) | 2026-04-30 | FAIL | CRITICAL: SkippedError lançado em executeAction L325/L449 é capturado por try/catch local (L351/L531), virando string em errors[] e re-lançado como Error genérico em L545 — instanceof SkippedError em L163 é sempre false → status final 'failed', NÃO 'skipped'. Cenário 2534023 (objetivo central do fix) continua marcando logs como 'failed'. Idempotency check L128-140 funciona corretamente. | Rex (dev-dev-beta) |
| FIX-IG-INLINE-ERROR-DISPLAY (working tree useConversas.ts + Conversas.tsx) | 2026-04-30 | PASS | nenhum | Nova (dev-dev-alpha) |
| FIX-ORA-EMPTY-INBOX-FWUP30 (working tree fwup30 + useConversasSimples.ts + client-migrations.json) | 2026-04-30 | CONCERNS | MEDIUM (**não-bloqueante** — workaround intencional dado fwup28 em prod): tryRpc envia null quando user pede 'open' para incluir NULL service_status sem aguardar deploy fwup30. Trade-off conhecido: bypass de filtro retorna closed/pending também. Para João Guirunas (maioria NULL, closed minoritário) é aceitável. **Pós-fwup30 propagado**, criar follow-up para reverter para `'open'` literal e remover o bypass. directQuery está correto. | Nova (dev-dev-alpha) |
| FIX-SENDS-IMPORT-PIPELINE-DUPLICADO (commit de11ae20) | 2026-04-30 | PASS | nenhum | gamma (dev-dev-gamma) |
| FIX-IG-AUTOMATION-IDEMPOTENCY (re-submetido commit f96a7920) | 2026-04-30 | PASS | nenhum | Rex (dev-dev-beta) |
| TASKFORCE-ORA-PUSH (commits 56a25ef1 + 30a94c47 + migration 10187 + reversão sends) | 2026-04-30 | FAIL → PASS | Round 1 FAIL: `sends-import-contacts` adicionou `.eq('tenant_id', tenantId)` em 3 queries de `lead_field_definitions` mas a tabela não tem essa coluna (modelo silo, cada tenant=Supabase project próprio). Round 2 PASS após reversão completa pelo data-engineer: função volta ao estado pré-fix (sem extração de tenantId via JWT, sem `.eq('tenant_id',...)` nas 3 queries em L286, L298, L312 — git status confirma sends-import-contacts limpo). Migration 10187 idempotente preserva 12 tipos prévios + adiciona email. MobileConversaSingle ambas ocorrências `'text'`→`'texto'`. Bug 4 padronização `error_reason` consistente entre WA/IG/omni-delivery + fallback `delivery_error` no UI para retrocompat. CONCERN documentado como story de hardening futuro: `whatsapp-outbound:931-933, 984-986, 988-997` faz overwrite total do metadata JSONB em vez de merge — Supabase JS não suporta merge JSONB nativo, correção requer RPC custom; aceito por Beta. | dev-dev-beta + dev-data-engineer |
| SP-01 (template XLS no ImportListaTab — working tree) | 2026-05-03 | CONCERNS | LOW (não-bloqueante, desvio de escopo aceitável): lead descreveu "selector de formato" mas implementação usa **dois botões lado a lado** ("Baixar CSV" / "Baixar XLS") em grid 2-cols — funcionalmente equivalente, visualmente coeso (mesmas classes h-[34px] text-[12px] border-dashed + FileDown). LOW (smoke não executado): não foi possível validar visualmente o .xlsx aberto em Excel/LibreOffice — recomenda-se download manual de pelo menos 1 dos 4 tipos antes do push para confirmar acentuação ("Recomendação") e estrutura. LOW pré-existente (não regressão): lint warning useMemo L155 missing dep 'user.profile'. Implementação técnica correta: aoa_to_sheet+book_append_sheet+writeFile uso canônico, parâmetro format default 'csv' preserva comportamento prévio, xlsx ^0.18.5 já em package.json, único arquivo alterado, 0 erros TS no arquivo. | (não atribuído) |
| BUGS-ORA-3FIX (commits 6154033c + cb64b466 + 35276fc0 + working tree send-dispatch-worker/useSendDispatch) | 2026-04-30 | CONCERNS | **Bug1** (omni stage_trigger): trigger SQL idempotente (CREATE OR REPLACE), `EXCEPTION WHEN OTHERS` evita travar leads, defesa em profundidade (G1 ai_enabled checado em 2 lugares). **Bug2-FE** (atomic save): RPC save_agent_complete substitui mutations separadas, IDs `etapa-*` strippados. **Bug2-BE** (humanizacao cast): migration 10190 idempotente, cast `::boolean` removido (humanizacao é text 'alta'/'media'/'nenhuma'). **Bug3** (sends FK): query separada para `settings_whatsapp_channels` resiliente a tenants sem FK, surface de erro real no client. CONCERN-1 (LOW): `AgenteSingle.tsx:82` introduz type mismatch `Etapa[] → EtapaPayload[]` (nome optional vs required). Não bloqueia build (strict:false). CONCERN-2 (LOW): stage_trigger não checa `ai_processing_lock` antes de processar — race teórica com message_buffer cron, baixa probabilidade em produção. | gamma (Bug1) + alpha (Bug2-FE) + beta (Bug2-BE + Bug3) |
| FIX-SENDS-TEMPLATE-VARS (commit 04926eaa) | 2026-04-30 | CONCERNS | LOW: fix só atua quando template tem `variables_map` em `json_data`; templates legados sem mapa caem no fallback hardcoded (`'2'` → `person.whatsapp`); LOW: `variables_map` parcial (mapeia só algumas posições) ainda permite leak nas posições não mapeadas | Rex (dev-dev-beta) |
| FIX-TLDV-SAVE (commit 04926eaa) | 2026-04-30 | CONCERNS | LOW: inconsistência nominal — seed migration usa `'tldv'`, código usa `'tl;dv'` (cosmético, não bloqueante); LOW: upsert sobrescreve `display_name` em row pré-existente (perde valor do seed) | Rex (dev-dev-beta) |
| FIX-AGENTES-PRO-SAVE (commit 4fa57674) | 2026-04-30 | PASS | nenhum bloqueante — fix em camadas (helper `uuid()` no client + `NULLIF(...)::uuid` na RPC + guards `isPending` nos useEffects); migration 10192 idempotente preserva fix 10190 (humanizacao sem cast boolean); manifest entry adicionada; lock `FOR UPDATE` em `ai_agents` previne dados duplicados ou race; `DELETE+INSERT` de `ai_agents_steps` é atômico dentro do lock | Nova (dev-dev-alpha) |
| ora-fix-agent-save-409 (migration 20260501130000) | 2026-05-01 | CONCERNS | CONCERN-1 (LOW, intencional): fallback silencioso para `created_by=NULL` quando todas as 3 branches do resolve falham — sugerido `RAISE NOTICE` em hardening futuro. CONCERN-2 (INFO): colisão UUID v4 em branch 1 teoricamente possível, probabilidade ~zero. CONCERN-3 (OPS): migration ainda **não aplicada** ao banco — Grav coordenar apply (db push ou CI/manifest). Frontend `useAgentesIAReal.ts:471` intencionalmente não muda — fix backend-only opção A. Rollback fiel verificado bit-a-bit. UPDATE de `ai_agents` + replace de `ai_agents_steps` preservados verbatim de 20260501120000. Sem regressão em criar (path separado) nem listar (RPC só INSERT/UPDATE). | Bythak (dev-data-engineer) |
| USER-TYPES-REFACTOR (USER-01..04, migration 20260502120000 + 26 arquivos) | 2026-05-02 | CONCERNS | 4 não-bloqueantes. CONCERN-1 (LOW): `PermissoesConfig.tsx:215,219` compara `role.name === 'gestor'` em `tenant_roles` RBAC (custom, separado de `user_type`). CONCERN-2 (LOW): `useUserPermissions.ts:65` `(user.profile as any).role_id` type-loose. CONCERN-3 (INFO): `useUsersNew.ts:183-187` backward-compat `gestor`→`user_type='manager'` intencional. **CONCERN-4 (LOW, regressão latente):** `useUserPermissions.ts:124-128` `getTeamFilter()` removeu branch `if (isConsultor && userTimes.length > 0) return userTimes` — agora retorna `""` para tipo `user`. `Conversas.tsx:352-358` perde auto-filtro de time. NÃO blocker imediato (`isReunioesModuleActive=false` hardcoded em Conversas.tsx:313). Restaurar via `if (basePermissions.isUser && userTimes.length > 0) return userTimes`. | byte (USER-01) + Rex (USER-02) + Sera (USER-03) + Nova (USER-04) |
| BI-VOICE-SANITIZER + BI-2 (commits 54289e29 + 4f32851f + working tree integração VoiceChatButton) | 2026-05-02 | PASS | 1 LOW pré-existente (`BIProInsightsTab.tsx:697` warning useCallback exhaustive-deps em sendMessage — fora do escopo, intencional). | Rex (sanitizer) + Nova (BI-2 SummaryBar+Insights) |
| ora-agents-pro-fix (voice remodel + elevenlabs-agent-sync hardening — working tree AgenteSingle.tsx + VoiceAgentConfigTab.tsx + index.ts edge fn) | 2026-05-02 | PASS | 3 LOW não-bloqueantes. LOW-OPS: edge fn `elevenlabs-agent-sync` não deployada — sem deploy, 4 fixes inertes. LOW-OBS: edge fn sem entrada em `supabase/config.toml` (status quo pré-fix, não introduzido). LOW-UX: `VoiceAgentConfigTab.tsx:339-348` `new Audio(...).play()` sem cleanup; múltiplos cliques sobrepõem preview (cosmético). | Alpha + Gamma (frontend) + Beta (edge fn) |
| MobileBiPro (working tree src/pages/mobile/MobileBiPro.tsx) | 2026-05-02 | PASS | nenhum bloqueante; `rounded-full` mantido em dot indicators e botões mic/send (semanticamente apropriado). | Nova (dev-dev-alpha) |
| SQUAD-B-OMNI-FIXES (Bug 1 forms + Bug 2 messages + Hardening) | 2026-06-08 | CONCERNS | HIGH-1: migration fwup32 single-pass não converge cadeias A→B→C (mitigado em runtime pelo hook recursivo); HIGH-2: realtime dual-channel multiplica subscriptions em chains profundas (raro na prática); MEDIUM-1: `omniChannel='whatsapp'` para contato sem nada continua semanticamente errado | squad-b-beta + squad-b-gamma + squad-b-delta (Axikar gate) |
| BI-3 + BI-4 (working tree BIProComercialTab.tsx + BIProRevOpsTab.tsx — PR-G2 CloserTable + PR-G3 RevOps Funnel 2D Linear-style) | 2026-05-02 | PASS | nenhum bloqueante. CustomerJourneyFunnel substituído de trapezoide 3D + cores hardcoded (FUNNEL_COLORS removido) por barras 2D enterprise (grid CSS, conector ArrowDown discreto, overallPct rodapé). HeroKPI sem gradient stripe; ProgressRing usa currentColor + text-primary. CloserTable padronizado com FilterChip pattern. | gamma (dev-dev-gamma) |
| ora-text-agents-fups (seed `11_ora_text_agents_fups.sql`) | 2026-05-02 | CONCERNS → **FAIL** (re-gate solicitado pelo lead) | **3 BLOCKERS** após cross-check com `tools/ora_setup_templates_followups.sql` e `meeting-followup-auto-setup/index.ts:365`. **BLOCKER-1**: `type='whatsapp_texto'` em 27 INSERTs viola convenção canônica do projeto (`type='whatsapp'` em ambos componentes oficiais; `whatsapp_texto` é alias legado/baseline). **BLOCKER-2**: Sec2 (3 regras `meetings_followups`) ignora padrão Meta Templates de `meeting-followup-auto-setup` — todas 3 vão FAIL-CLOSED em runtime no worker `process-meeting-followups:458-468`. Templates Meta já existem em `tools/ora_setup_templates_followups.sql:88-95` (`ora_agend_confirmacao`, `ora_agend_lembrete`, `ora_agend_link`). **BLOCKER-3**: `type='ligacao'` em Sec1 (4 registros) é ruído — documento define D5/D6/D7 como ligação via discador externo, fora do escopo texto. Worker repassa ao N8N indiscriminadamente; cliente recebe "Tentativa de ligação D+6 — corretor toma nota" no WhatsApp. **CONCERNS LOW**: D3 pessoal/evento/network são copies do data-engineer; stage UUID hardcoded sem guard. **PASS** em: zero schema changes, idempotência, tenant correto, sintaxe, control text (FWUP-24). Detalhes em `ora-text-agents-qa-verdict.md`. | byte (dev-data-engineer) |
| ora-elevenlabs-agent-sync (re-gate fluxo save→sync — AgenteSingle.tsx + VoiceAgentConfigTab.tsx + edge fn) | 2026-05-02 | PASS | 4 LOW não-bloqueantes. **LOW-UX-1**: `eleven_v3` listado em VOICE_MODELS mas edge fn silenciosamente substitui por `eleven_flash_v2_5` via `sanitizeTtsModel` — usuário escolhe v3 e sync usa flash sem aviso. **LOW-UX-2**: `new Audio(...).play()` sem cleanup permite áudios sobrepostos em cliques rápidos (cosmético, persistente do round anterior). **LOW-OBS-1**: edge fn sem entrada em `supabase/config.toml` (status quo, não regressão). **LOW-RACE**: `performSync` catch externo (network error antes de chegar ao EL) seta `el_sync_status='error'` apenas em estado local; DB pode persistir 'pending' do save anterior até próximo refetch. Aceitável (cache invalidado). | Alpha (frontend) + Beta (edge fn) |
| Coach Pro CP-4 + CP-5 (epic Phase 3 final — CoachProConfig single-screen + Reunioes lista com dados Coach inline) | 2026-05-03 | **CONCERNS** | **3 issues — 2 LOW, 1 MEDIUM (escopo declarado, não bloqueante para demo).** **CP-5: 10/10 ACs PASS.** Hook `useAgendamentosSimple.ts` faz JOIN nested em `meeting_evaluations` com seleção de versão ativa via `superseded_at IS NULL` (fallback `evaluation_version`); zero N+1. Cluster Coach inline na tabela: coluna `Lead` (renomeada AC7) + `Coach` (ScoreBadge + Brain icon violet) + `Risk` (DealRiskBadge compact), todas condicionais `isCoachActive`. Filtros chips multi-select `coachScoreFilter`/`dealRiskFilter` com 5+4 buckets; pills ativas com `clear()` toggle individual. Sort `coach_score` no enum (asc=problemas/desc=top calls); reuniões sem avaliação caem para final via `?? -1`. Helper `scoreBucket()` consistente com thresholds canônicos ≥8/≥6/≥4. Zero regressão. **CP-4: 8/10 ACs PASS, 2 NÃO ENTREGUES.** Layout single-screen confirmado: sidebar 260-280px + PlaybookEditor + SettingsBlock accordion 3-up no rodapé (context/notif/assign com sumário inline). `useDebouncedSave.ts` (novo, 58 linhas) genérico com states idle/pending/saving/saved/error. CriterionEditor + SectionEditor + PlaybookEditor migram inputs h-[26px] → h-9/h-8 (AC5). Soma de pesos com cores OK/OVER/UNDER em SectionEditor + PlaybookEditor stats strip (AC4). Sidebar com search + grouping `Templates de sistema` colapsável + `Meus playbooks` por tipo + active state `bg-primary/10` (AC6). Empty state com BrainCircuit + CTA "Novo Playbook" (AC9 — sem variante "Clonar template Discovery B2B" da spec). Mutations preservadas (AC10). AlertDialog de delete + lógica `is_default_for_type` via `handleDefaultForType` preservada. **CONCERN-1 (MEDIUM, AC3 não entregue):** ScorecardPreview component **não existe** no codebase — `find src/components/coach/` retorna apenas `coach-tokens.ts` + `coach-utils.tsx`. Tabs internas Editor/Preview ausentes. Não bloqueia demo (UI funciona sem preview), mas é entregável declarado fora. **CONCERN-2 (LOW, AC8 parcial):** mobile responsive incompleto. Sidebar fixa `w-[260px] md:w-[280px]` em vez de virar tabs sequenciais `[Lista] [Editor] [Preview]` abaixo de `md`. Layout funcional mas apertado em mobile. **CONCERN-3 (LOW, ESLint):** `CoachProConfig.tsx:527` warning exhaustive-deps `criteria` em useMemo de `sumCriteriaWeights`; `Reunioes.tsx:271` warning idem `scoreBucket`; `Reunioes.tsx:714` error `(pill: any, i)` pré-existente. tsc EXIT 0; `vite build` EXIT 0 (chunk CoachProConfig 87.56 kB / 12.22 kB gzip). Zero schema migrations no escopo. | gamma (CP-4 + CP-5) |
| Coach Pro CP-1/2/3/6/7 — smoke pós-fix CONCERN-0 + CONCERN-1 (seed 0-10 + ScoreEvolutionChart Recharts + MeetingRecordCard /10) | 2026-05-03 | **PASS** | 2 LOW remanescentes não-bloqueantes (CONCERN-2/3 do gate anterior). Smoke estático confirma: (1) zero divisões /100 espúrias nas 5 telas; (2) thresholds ≥8/≥6/≥4 unificados em CoachDashboard/CoachTeamBoard/CoachConsultantProfile/ReuniaoSingle/CoachMeetingEvaluation + coach-utils + coach-tokens; (3) `ScoreEvolutionChart` agora em Recharts ComposedChart com domain `[0, 10]` em CoachDashboard.tsx:107 e CoachConsultantProfile.tsx:65; (4) MeetingRecordCard.tsx:71 corrigido `score / 100`→`score / 10` + label "/100"→"/10" + thresholds 70/40→7/4 + default state ai_score 50→5; (5) seed 08_coach_pro.sql `v_scores` migrado int→numeric(4,2) com escala 0-10 (8.5/7.0/9.2…) + verdict thresholds 88/72/60→8.8/7.2/6.0 + risk 85/70→8.5/7.0 + sentiment_arc usa GREATEST/LEAST com offsets 0.8/0.6 escalados. CoachTeamBoard mantém Sparkline SVG inline (decisão de spec — micro-viz inline 60×20px, alternativa Recharts seria over-engineering). Hooks `useCoachTeam.ts`/`useCoachEvaluations.ts` consomem `overall_score` direto sem rescale; `Math.round(avg * 10) / 10` é arredondamento decimal (não rescale). tsc EXIT 0; ESLint 0 issues nos arquivos do escopo. | Alpha (CONCERN-1 ScoreEvolutionChart + MeetingRecordCard) + dev-data-engineer (CONCERN-0 seed 08_coach_pro.sql) |
| Coach Pro CP-4 AC3 (ScorecardPreview tab — re-gate da AC3 não entregue no gate anterior) | 2026-05-02 | **PASS** | 1 LOW (Preview omite descrição/exemplos do critério presentes no editor — interpretação razoável dado que ScorecardPreview simula visão do consultor; documentar). | gamma (dev-dev-gamma) |
| ORA-PROMPT-REWRITE-INBOUND (`supabase/tools/ora_update_agent_prompt.sql`) | 2026-05-03 | **FAIL → PASS** | **Round 1 FAIL**: Objeção 6 violava REGRA INVIOLÁVEL — terminava com "Prefere amanhã ou depois de amanhã?" sem estar listada como exceção. **Round 2 PASS** após fix do gamma: regra inviolável (linhas 24-30) agora lista 2 exceções explícitas — Obj 6 (opções de DIA) e Obj 13 (decisão conjugal). Obj 6 (linhas 131-135) mantém fechamento "Prefere amanhã ou depois de amanhã?" coerente com a exceção declarada. Outras 11 objeções (1-5, 7-12) e Obj 13 cumprem fechamento canônico "comercial ou à noite?". 9 passes preservados sem regressão: 1h em todo prompt, Obj2/Obj5 com versões recomendacao + pessoal/evento/network, fluxo agendamento (período → `consultar_disponibilidade` `p_slot_minutes=60` → `enviar_opcoes_horario` max 20 chars → `criar_agendamento` `start_time`+`end_time` ISO 8601, end=start+60min, sem `duration_minutes`), Red Flags via `bloquear_ia`, Scripts de Abertura removida, Primeiro contato inbound com 4 versões, 5 tools válidos no codebase. **CONCERN-1 (LOW, cosmético, não bloqueante):** linha 223 em REGRAS GERAIS ainda diz "Após toda objeção (1-12)" sem mencionar exceção da Obj 6 — inconsistência com seção autoritativa REGRA INVIOLÁVEL (24-30) que lista exceções explicitamente. LLM lerá ambas; risco prático baixo. Hardening futuro: ajustar para "objeção (1-12, exceto 6 e 13)". | gamma (dev-dev-gamma) |
| ora-fase2-melhorias (Scope 1 prompt v2 + Scope 2 dadosEntradaModels + Scope 3 ai-agent-execute responsavel) | 2026-05-03 | **PASS** | 22/22 itens individuais do checklist verificados. **Scope 1** (`supabase/tools/ora_update_agent_prompt.sql`): `{{controle}}` e `{{relacao_erika}}` zero ocorrências; abertura PESSOAL usa `[relacao_corretor]` (L52); bloco PRÉ-AGENDAMENTO (L68) precede FLUXO DE AGENDAMENTO (L76), instrui `collect_identity` com `field="email"` (L72); REGRA INVIOLÁVEL (L24-30) lista exceções obj 6 (DIA) e obj 13 (conjugal); todas 13 objeções com fechamento canônico — obj 1-5/7-12 com "comercial ou à noite?", obj 6 com "amanhã ou depois de amanhã?" (L144), obj 13 com "comercial ou à noite?" (L185); reunião 1h consistente (L21, 85, 89, 233 — `end_time = start_time + 60 min`); 6 tools válidas (atualizar_lead, bloquear_ia, criar_agendamento, consultar_disponibilidade, enviar_opcoes_horario, collect_identity); status enum `in_progress|won|lost` enforcement explícito (L237). **Scope 2** (`dadosEntradaModels.ts`): seção `id:'origem'` posicionada entre 'lead' (L30) e 'pessoa' (L66) na L53; 5 campos esperados (origem_lista, recomendante, relacao_recomendante, relacao_corretor, nome_evento) em L58-62; seção 'lead' com 3 novos campos `lead_responsavel_nome/email/telefone` (L43-45); `PADRAO_COMPLETO` filtra apenas `s.id !== 'qualificacao'` (L152) — inclui 'origem'; `SO_BASICOS` `secoesAtivas` Set contém apenas `mensagem|lead|pessoa` (L159) — exclui 'origem'. **Scope 3** (`ai-agent-execute/index.ts`): ContextData tipado com `lead_responsavel_email: string` e `lead_responsavel_telefone: string` (L89-90); query condicional `if (lead?.user_id)` em settings_users selecionando `id, name, email, phone` (L704-712); ambos os ramos else (lead sem user_id em L713-717 e sem lead em L744-746) zeram os 3 campos. Sem regressão. | gamma (Scope 1 prompt) + Alpha (Scope 2 frontend) + Beta (Scope 3 backend) |
| ora-validate-user-types (gate transversal de modelo de roles + RLS + guards) | 2026-05-07 | **FAIL → CONCERNS → PASS** | **Round 1 FAIL** (1 CRITICAL + 3 HIGH + 3 MED + 6 LOW): bypass RLS auto-promoção em `settings_users` via `authenticated_*` USING(true) da FWUP-17, coluna fantasma `tenant_id` em `admin-unenroll-mfa`, invariante `super_admin↔user_type` sem trigger, 2 stacks RBAC mortos (user_roles + tenant_roles). **Round 2 CONCERNS** após FIX-USR-01/02/03 aplicadas: FIX-USR-01 (`20260507160901`) DROPA policies abertas e cria 4 restritivas (`is_admin()` SECURITY DEFINER + WITH CHECK no UPDATE bloqueia owner de setar `super_admin=true` ou `user_type='admin'` em si próprio — auto-promoção rejeitada matematicamente). FIX-USR-02 remove `tenant_id` de `admin-unenroll-mfa/index.ts:48`. FIX-USR-03 (`20260507161250`) cria trigger `trg_settings_users_sync_admin_flag` BEFORE INSERT/UPDATE forçando invariante bidirecional + backfill (lead reportou 0 rows violando). FE alinhado: `useAuth.ts:196` agora `super_adm = super_admin === true OR user_type === 'admin'`. HIGH-3 INVALIDADO pelo lead (tenant_roles em uso ativo, FIX-USR-04 cancelada); `user_roles`+`app_role`+`has_role()` rebaixados para LOW-7. ADR-USR-01 (FWUP-17 retroativo + ciclo FIX) marcado completed pelo lead. REMANESCENTES não bloqueantes: MED-1 `currentTenantId='single-tenant'` hardcoded, MED-2 `types.ts` drift, MED-3 cobertura zero de testes (reforçado — sem regression test FIX-USR-01 pode ser revertida em manutenção), LOW-1..7 hygiene. **Round 3 PASS** após verificação lógica exaustiva do WITH CHECK do `settings_users_update_owner_or_admin` confirmar bloqueio matemático completo dos 4 cenários relevantes (auto-promoção user_type/super_admin, edição não-crítica, admin) — defense-in-depth restaurada via tripla cobertura (RLS + trigger invariante + FE alinhado). HIGH-3 invalidada formalmente (tenant_roles em uso ativo, decisão arquitetural delegada para ARCH-RBAC-01); user_roles+app_role+has_role() rebaixados para LOW-7. **Push LIBERADO**. Gate `ora-validate-user-types` ENCERRADO. Veredicto detalhado em `user-types-verdict.md` (Round 1 + Round 2 + Round 3 preservados). | Round 1: dev-analyst (mapping) + dev-data-engineer (schema audit) + Axikar (gate); Round 2/3: Axikar + Bythak (FIX-USR-01/02/03) + dev-architect (ADR-USR-01 + ARCH-RBAC-01/ADR-AUTH-09) |
| NYLAS-08 (round 2 — fix HIGH-1 + HIGH-2, commit 2cab8da) | 2026-05-07 | **CONCERNS → PASS** | **2 HIGH resolvidos.** **HIGH-1 RESOLVIDO**: `nylas-availability/index.ts` agora tem `CallerRow.userType: string` (L394), `.select('id, user_type')` (L403), `userType: (data.user_type as string) ?? 'user'` (L412), `adminTypes = new Set(['manager','admin'])` (L420) — sem `'global_admin'`. Único `role:` remanescente é nome de chave em log estruturado (L139), valor é `callerRow.userType` correto. Schema canonical alinhado (`user_type` admin/manager/user pós migration `20260502120000`). **HIGH-2 RESOLVIDO**: `useConsultorDisponibilidade.ts:fetchNylasProviderInfo` (L26-61) e `useAgendamentos.ts:resolveCalendarProvider` (L50-89) ambos com query paralela `settings.calendar_provider` em `Promise.all` + early-return `if (globalSettings?.calendar_provider !== 'nylas') return { useNylas: false, grantId: null }` antes de avaliar per-user flag/grant. **Verificação dos 4 pontos requeridos**: (1) query correta — table `settings`, column `calendar_provider`, ordering `created_at DESC LIMIT 1` para single-tenant single-row pattern; (2) guard `!== 'nylas'` cobre null, 'legacy_google' e qualquer valor futuro fora do enum; (3) hooks simétricos — pattern idêntico aplicado em ambos `Promise.all` com mesma ordem de destructuring `[globalSettings, settingsRow, connectionRow]`; (4) happy path preservado — quando `calendar_provider==='nylas'`, fluxo segue para checagem `use_nylas_calendar && nylas_grant_status==='valid'`. **Ordering correto**: `useAgendamentos.ts:53` `if (!consultorId) return` ANTES do `Promise.all` (L55); `globalSettings` corretamente destructurado como `{ data: globalSettings }` da primeira posição. `vite build` EXIT 0 (validação de tipos via build, projeto sem script `typecheck`). ESLint 0 erros novos nos 3 arquivos do escopo (linhas que remanescem com warnings são pré-existentes em outros arquivos do repo). Migration `20260507210000_nylas_09_calendar_provider.sql` confirma coluna existe com check constraint `IN ('legacy_google', 'nylas')` default `'legacy_google'`. **MEDs/LOWs remanescentes do round 1** (não bloqueantes, tracking apenas): MED-1 `useSettings` retorna `nylas_api_key` raw para admin (FUP cross-cutting); MED-2 `loadNylasSettings` 500 vaza nome de campo missing; LOW-1 `mapNylasError` sem retry-after granular; LOW-2 `nylas-webhook` 200 em parse JSON inválido pós-HMAC. **WAIVEDs do round 1** (sandbox dependentes — AC3-AC6 smoke): mantidos pendentes — destravarão quando NYLAS-01 AC5 (humano provisionar sandbox Nylas) for concluído. **Push do epic ora-nylas-calendar LIBERADO** quanto aos 2 HIGH bloqueantes; AC2 SQL forensics e AC3-AC6 smoke aguardam sandbox + execução manual. | dev-backend-2 (HIGH-1) + dev-frontend-1 (HIGH-2) + Axikar (gate) |
| NYLAS-08 (gate epic ora-nylas-calendar — integração Nylas end-to-end) | 2026-05-07 | **CONCERNS** (round 1) | **2 HIGH + 2 MED + 2 LOW + 4 WAIVED.** Sandbox Nylas não provisionado (NYLAS-01 AC5 ação humana pendente) → AC3-AC6 (smoke availability/create/update/delete/OAuth UI) WAIVED. **HIGH-1**: `nylas-availability/index.ts:403` faz `.select('id, role')` em `settings_users` — coluna `role` NÃO EXISTE no schema canonical (apenas `user_type` admin/manager/user pós migration `20260502120000_user_types_canonical_refactor.sql` + `role_id` FK para `tenant_roles`). Em runtime, qualquer chamada à edge function retorna 500 `caller_lookup_failed` (Postgres rejeita query). Refactor: `.select('id, user_type')` + ajustar `adminRoles = new Set(['admin','manager'])` removendo `'global_admin'`. **HIGH-2**: gate global `settings.calendar_provider` só aplicado em `useGoogleCalendarStatus.ts:28`; `useConsultorDisponibilidade.ts:fetchNylasProviderInfo` (L26-51) e `useAgendamentos.ts:resolveCalendarProvider` (L50-79) decidem branch Nylas vs Google **apenas** por `settings_users.use_nylas_calendar` per-user. Viola AC7b: rollback admin para `legacy_google` não bloqueia Nylas para users com flag per-user true + grant válido. **MED-1**: `useSettings` retorna `nylas_api_key` raw para admin (RLS-protected mas exfiltrável via XSS) — FUP cross-cutting (vale para google/meta secrets também). **MED-2**: `loadNylasSettings` 500 vaza nomes de campos missing (`Nylas credentials missing in settings: nylas_api_key`) — minor info leak, sugerido mensagem genérica + log server-side detalhado. **LOW-1**: `mapNylasError` não loga retry-after granular. **LOW-2**: `nylas-webhook` retorna 200 em parse JSON inválido pós-HMAC (decisão consciente para evitar retry storms, mas falta alerta). **PASS items**: AC1 segurança OAuth — CSRF state UUID v4 + TTL 10min + one-shot delete (`nylas-auth-connect/index.ts:113-118,252`); HMAC-SHA256 webhook constant-time comparison (`nylas-webhook/index.ts:258-283`); ownership + grant-mismatch check em events-sync (`nylas-events-sync/index.ts:198-237`); `PRIVILEGED_USER_TYPES = Set(['admin','manager'])` alinhado com canonical user_types; tokens NULL forçados no upsert nylas (linhas 239-241); RLS default-deny em `nylas_oauth_states` + `nylas_webhook_events`; mascaramento UI credenciais (`••••••••••`); `nylas_api_key` não vaza no bundle frontend (apenas chave de form, nunca env-baked). AC2 token storage validado por code review (SQL waived — sub-agent sem MCP supabase exec_sql); AC8 error states 412/429/500 propagados com mensagens claras + ações ("Reconectar"); AC9 fallback Google legado preservado (`useAgendamentos` chama `google-cal-upsert-event` quando `useNylas=false`). **Próximos passos:** dev-backend-2 fix HIGH-1; dev-frontend-1 fix HIGH-2; dev-data-engineer rodar SQL AC2 em `wotuyxscsfralqpoiyfv`; humano provisionar sandbox; QA round 2 pós-fixes + sandbox para fechar epic. Veredicto detalhado em `nylas-verdict.md` + checklist em `nylas-oauth-security-checklist.md`. | dev-backend-2 (NYLAS-03/04/05) + dev-frontend-1 (NYLAS-06/07/09) + Axikar (gate) |
| WEBHOOK-TEMPLATE-FIX (`webhook-inbound/index.ts:267` `source_type: 'webhook'` → `'form'`) | 2026-05-10 | **PASS** | Fix cirúrgico de 1 linha + comentário inline. Causa raiz validada: constraint `messages_source_type_check` (live DB) lista `('inbound','manual','ai_agent','campaign','form','followup','appointment_reminder')` — `'webhook'` NÃO está → INSERT falhava no `messages` table com try/catch externo capturando o erro silenciosamente, gerando `webhook_trigger.insert_failed` log + `trigger_dispatched: false` no response. Fix muda valor para `'form'` (mesmo precedente de `lp-submit/index.ts:1036/1208/1250` — origem real é discriminada por `module_ref_id` apontando para `inbound_webhooks.id`). 8/8 checklist verificado: (1) cirúrgico, sem refactor; (2) sem novos testes (escopo 1 linha); (3) ACs cobertos — webhook com `trigger_config.enabled=true` agora consegue enfileirar mensagem; (4) sem regressão — `useCanSendMessage.ts:173-181` filtra `from_contact='cliente'` AND não-form, mas trigger usa `from_contact='sistema'` → janela 24h intocada; (5) performance idem; (6) segurança idem — `module_ref_id` continua sendo discriminador único de origem; (7) comentário inline auto-documenta o porquê em L266; (8) contratos `inbound_webhooks` e response body inalterados. **Edges verificados:** (a) `trigger_config.enabled=true` + `wa_template_id` removido → guard `webhook_trigger.template_not_found` em L204-209 dispara, retorna false, sem INSERT. (b) Webhook sem campo whatsapp → guard `webhook_trigger.no_whatsapp` em L173-175. (c) `action === 'skipped'` (linha 508) → `maybeDispatchTrigger` NEM é chamado, está correto: nada criado/atualizado, nada para disparar. (d) `create_mode='criar'|'criar_se_nao_existir'|'atualizar_etapa'|'somente_etapa'` → fluxo bifurcado anterior intacto, dispatch só ocorre fora do `skipped`. **Mea culpa:** o veredicto WEBHOOK-INCREMENT (linha 92, 2026-05-09) deu CONCERNS sem verificar que `'webhook'` violava a constraint live — gap que `feedback_runtime_smoke_before_gate.md` cobre. Memory `feedback_git_state_freshness.md` e `feedback_runtime_smoke_before_gate.md` aplicáveis: gate atual cruzou constraint live + precedente `lp-submit`. Push LIBERADO. | dev-dev-beta (fix) + dev-data-engineer (audit schema/trigger_config) + Axikar (gate) |
| PIPE-2.1 (campo `curso` global + RPC append atômica + crm-mapper roteamento) | 2026-05-27 | **CONCERNS → PASS** (Round 2) | **Round 1 CONCERNS:** CONCERN-1 MEDIUM race em concat JS (lookup-before-write em `readCustomFieldValue` → `computeAppendedValue` → `upsert_crm_field_value` permite lost-update entre 2 webhooks paralelos para mesma pessoa com cursos diferentes); CONCERN-2 LOW `readCustomFieldValue` sem filtro `pipeline_id IS NULL`; CONCERN-3 LOW sem testes unitários de `computeAppendedValue`. **Round 2 PASS:** Rex implementou durante o gate a RPC `append_crm_field_value` (migration `20260527150000`) que serializa o read-modify-write via `pg_advisory_xact_lock(hashtextextended(person_id||'|'||def_id, 0))` — lock per-(pessoa,campo) released no COMMIT, zero contenção cross-row, lost-update matematicamente impossível. `crm-mapper.persistCustomFields` (L391-419) virou 1 if/else simples sobre `APPEND_ON_CONFLICT_FIELDS`, sem helpers JS (toda lógica DB-side). Dedup case-insensitive via `regexp_split_to_table('\s*;\s*')` + `lower(btrim(token))`. 6/6 cenários do AC6 verificados contra SQL da RPC. **Remanescentes LOW (não bloqueantes):** (1) LOW-1 herança — `append_crm_field_value` e `upsert_crm_field_value` ambos buscam definition sem filtro `pipeline_id IS NULL`; hoje seguro porque unique constraint `(key, entity_type, pipeline_id)` NULLS NOT DISTINCT + row global; (2) LOW-2 doc `webhook-inbound-curso.md` L117 ainda diz "regra em código" — drift cosmético vs implementação que migrou para SQL. **AC5 WAIVED:** smoke runtime via curl real depende de devops/lead executar contra Supabase Live; QA não pode (read-only). **Memory aplicada:** `feedback_git_state_freshness.md` (re-check de git state antes de fechar veredicto em rodadas múltiplas — descobri a RPC nova mid-gate). Push LIBERADO. Veredicto detalhado na seção QA Results de [[../../stories/active/PIPE-2.1-custom-field-curso]]. | Rex (dev-dev-beta) |
| PIPE-3.1 (UNIQUE partial indexes em dedup + default `create_mode='criar_se_nao_existir'`) | 2026-05-27 | **PASS** | **0 bloqueantes.** Migration `20260527140000_pipe_3_1_dedup_constraints.sql` tem pre-flight check robusto (4 queries de duplicação com `RAISE EXCEPTION` orientativa apontando para `merge_persons` RPC), cria 4 UNIQUE partial indexes idempotentes (`IF NOT EXISTS` + `COMMENT ON INDEX`): `idx_clients_people_email_unique WHERE email IS NOT NULL AND status <> 'merged'`; `idx_clients_people_whatsapp_unique WHERE whatsapp IS NOT NULL AND status <> 'merged'`; `idx_leads_people_pipeline_active_unique WHERE people_id IS NOT NULL AND leads_pipelines_id IS NOT NULL AND status NOT IN ('won','lost','archived')`; `idx_form_pro_submissions_meta_leadgen_unique WHERE meta_leadgen_id IS NOT NULL`. Status canônicos `'won'|'lost'|'archived'` confirmados via `20260228010000_p8_leads_status_normalize-ok.sql:35-41` + `20260501100000_fix_leads_status_ativo.sql:22-28` — constraint `leads_status_check` rejeita ativamente pt-BR, então filtro do index está completo. `ALTER ... SET DEFAULT 'criar_se_nao_existir'` sem backfill — rows existentes preservam decisão humana (retrocompatível). Rollback `20260527140000_pipe_3_1_dedup_constraints.rollback.sql` DROPs idempotentes + revert do default + warning se algo permanecer. Trade-off `crm-mapper` aceito explicitamente pelo lead (race ocasional → 500 em vez de duplicata silenciosa; PIPE-3.2 endereçará idempotência via `INSERT ... ON CONFLICT DO UPDATE`). Relatório `docs/smart-memory/agents/research/dedup-analysis.md` é completo (3 fluxos mapeados, 6 issues P0..P3, queries de validação no anexo). ⚠ Nota documental (não bloqueante): story PIPE-3.1 linha 96 lista `'ganho'/'perdido'/'arquivado'` (pt-BR) no campo "Riscos" como argumento de coverage — migration final usa apenas en, que está correto porque o DB rejeita pt-BR. Drift de doc vs implementação final, zero impacto runtime. Push LIBERADO. Veredicto detalhado na seção QA Results de [[../../stories/active/PIPE-3.1-dedup-leads]]. | Rex (dev-dev-beta) |
| Squad D Code Review (consolidado: arch + tech-debt + frontend + backend + fullstack + security + UX) | 2026-06-08 (rev 2026-06-08 round 2) | **FAIL → CONCERNS** | **TODOS OS 4 CRITICAL ORIGINAIS RESOLVIDOS** durante o gate. Round 1: CRIT-2 agents-trigger-call ✅ via remoção pela Squad A. Round 2 (squad-d-delta): CRIT-1 data-deletion ✅ (registro pending + EMAIL_RE whitelist + rate-limit + audit log); CRIT-3 logs-proxy ✅ (super_admin gate + whitelist regex `[a-zA-Z0-9 .,:;@/=#-]{0,100}` + audit log); CRIT-4 update-user-password ✅ (targetAuthId derivado server-side de targetUser.auth_user_id via userId lookup — vetor de mismatch fechado; permission matrix em camadas; audit log). Permanecem 24 HIGH + 37 MEDIUM + 18 quick wins como débito de hardening. **Push tecnicamente LIBERADO.** Observação LOW (follow-up): `update-user-email` ainda aceita `targetUserAuthId` no body (mesma classe de vetor de CRIT-4) — registrar como HIGH-S7. Veredicto detalhado em `docs/smart-memory/ops/squad-d-final-verdict.md`. | squad-d (architect/analyst/alpha/beta/gamma/delta/ux) + squad-d-delta (Kronix - patches round 2) |
| REMOVAL-COACH-CALLPRO (Squad A — drop módulos Coach Pro + Call Pro do frontend/edges/DB) | 2026-06-08 | **FAIL → PASS** (Round 2) | **Round 1 FAIL (6 CRITICAL + 4 HIGH):** build falhava, 7 imports órfãos em 4 arquivos, CallMegaConfig importava hooks deletados + chamava `call-pro-webhook`, `utils/modules.ts:26` mantinha `COACH PRO™`, send-dispatch-worker chamava `agents-trigger-call`, useUserPermissions/useNotificationPreferences/gemini-bi-instructions com refs Coach. **Round 2 PASS** após squad-a-gamma reaplicar cleanups cross-cutting + squad-a-beta remover Voice AI branch + squad-a-alpha-2 finalizar CallPro completo. Verificação Round 2: `npm run build` exit 0 (✓ built in 8.70s); grep amplo `coach\|Coach\|callpro\|CallPro\|call-pro` em src/ exceto types.ts/i18n → ZERO hits; grep edge fns deletadas → ZERO hits; grep `agents-trigger-call\|isVoiceSend\|voiceAgentId` em send-dispatch-worker → ZERO hits; 6 arquivos/diretórios confirmados ausentes (`src/components/coach/`, `src/components/call-pro/`, `src/pages/CoachDashboard.tsx`, `src/pages/CallPro.tsx`, `src/types/coach.ts`, `src/types/call-pro.ts`). Migration `20260609000000_drop_coach_pro_and_call_pro.sql` cobre 15 tabelas + view `v_coaching_insights` + 2 functions + ALTER PUBLICATION + DELETE `module_key IN ('coach','call')` + UPDATE `adm_clients.enabled_modules`. 7 edge fns deletadas (`coach-evaluate/email/weekly-summary`, `call-pro-webhook`, `agents-call-data/trigger-call/test-call`); `followup-status-callback` corretamente mantida (squad-a-beta corrigiu divergência do brief seguindo plano oficial). **WAIVED:** `src/integrations/supabase/types.ts` ainda declara `call_pro_calls/settings/meeting_evaluations/playbook_*` — plano §6 manda regenerar via `supabase gen types` **pós** apply da migration; build passa porque nenhum código referencia mais. Push LIBERADO; squad-a-devops autorizado para commit + PR. Veredicto detalhado em `docs/smart-memory/ops/squad-a-qa-verdict.md`. | squad-a-alpha-1 (Coach FE) + squad-a-alpha-2 (CallPro FE completo) + squad-a-beta/Rex (edge fns + send-dispatch-worker cleanup) + squad-a-gamma/Serak (routing + reaplicação cross-cutting) + squad-a-data (migration) |
| Coach Pro CP-1/2/3/6/7 (epic ora-coach-pro-ux-full — refinamento enterprise 5 telas + hooks + types + utils) | 2026-05-02 (revisado 2026-05-03) | CONCERNS+DATA-FIX-NEEDED | **6 issues — 1 bloqueia DEMO (data, não código), 5 não-bloqueantes.** **CONCERN-0 (CRITICAL/data integrity, identificado pelo team-lead pós veredicto inicial)**: seed `supabase/seeds/demo-2026-05-02/08_coach_pro.sql:94` popula `meeting_evaluations.overall_score` em escala 0-100 (`ARRAY[85,70,92,...]`) enquanto contract dominante do sistema é 0-10 (3 edge functions formatam `${score}/10`: bi-insights-chat:84, coach-email:384/434; ScoreRing CoachMeetingEvaluation.tsx:127 calcula `(score/10)*100`; ScoreBadge thresholds `≥8/≥6/≥4` em coach-utils.tsx:7-9; CoachDashboard.tsx:115-116 maxVal=10). Em runtime: gráficos saem do topo, ScoreRing sempre 100% (clipa), KPIs mostram "82.3" em vez de "8.2", thresholds visualmente quebrados. **Código Phase 2 está correto contra o contract** — bug está no seed (responsabilidade dev-data-engineer). Bloqueia demo válida; não bloqueia push técnico. Decisão A/B pendente do Chief: (A recomendada) corrigir seed → escala 0-10 + corrigir MeetingRecordCard.tsx:71 legacy `(score/100)` → `/10`; (B) inverter — alinhar UI/edge fns/thresholds para 0-100 (mais invasivo, contraria comunicação `/10`). **CONCERN-1 (MEDIUM)**: CoachDashboard `ScoreEvolutionChart` (148 LOC SVG inline) e CoachTeamBoard `Sparkline` não migraram para Recharts apesar do briefing/spec; CP-3 e CP-7 migraram corretamente. Inconsistência com BIPro. **CONCERN-2 (LOW)**: `useCoachTeam.ts:164` inicializa Map sem `recentScores: []` (tsc strict:false não pega; risco runtime defensivo improvável). **CONCERN-3 (LOW)**: `CoachConsultantProfile.tsx:605` cast `as unknown as` para `talk_ratio_consultant` que não está no tipo `ConsultantEvaluation` nem é selecionado pela query — KPI Talk Ratio do HeroStrip mostra `—` em runtime. **CONCERN-4 (LOW)**: `playbook_breakdown.best_criterion/worst_criterion` hardcoded `null` em `useCoachTeam.ts:374-382` (UI atual não consome, mas tipo declara). **CONCERN-5 (LOW)**: `EvaluationStatus` declarado em `types/coach.ts:4` mas `useCoachMeetingAssignment.ts:23` e `ReuniaoSingle.tsx:108` mantém `status: string`. **Pontos fortes**: tokens 100% migrados (zero bg-zinc-*/border-zinc-* nas 5 telas), 2 bugs do baseline corrigidos (CP-7 filter por section_id agora real via `criterion.section_id` em types; CP-3 flatMap morto substituído por `criteria_breakdown` server-side com top 3 strengths/gaps), score thresholds unificados (≥8/≥6/≥4/<4 todas as telas), `recent_scores: number[]` adicionado em `ConsultantMetrics` para sparkline, ScoreRing RadialBar 270° + SectionMiniRadar + Timeline de critérios em CP-7. tsc EXIT 0; ESLint 0 errors. Sem migrations novas. **Lição QA registrada em agent-memory:** `feedback_runtime_smoke_before_gate.md` — gates de UI exigem smoke runtime visual com dados reais, não só validação contra contract de tipos (foi o gap que permitiu CONCERN-0 escapar do veredicto inicial). Veredicto detalhado em `coach-pro-cp1235-67-verdict.md`. | Alpha (CP-1/CP-2/CP-3) + Beta (CP-6/CP-7) + dev-data-engineer (CONCERN-0 seed) |

## Notas

- **AUTH-V2-03c**: 6/6 ACs atendidos; mecanismo de step-up entregue mas sem wire-up em ação sensível concreta — fundação. RPC `mfa_recovery_consume` validado. Re-review (2026-04-26) identificou HIGH de atomicidade: `consumeRecoveryCode` marca `used_at` antes de `unenrollSelf` client-side. Se passo 2 falha, código queimado mas factor permanece. Recomendado AUTH-V2-03c-fixup movendo unenroll para dentro do RPC (atomicidade transacional).
- **BI-VOICE-00**: 5/5 ACs in-scope atendidos (AC6 deferido). Defesa em depth presente: helper service_role-only correto, SELECT client sem `api_key`, `AIProviderInput` segregado em mutations, `useUpdateAIProvider` não retorna a row após UPDATE. Concern de segurança é a RLS row-level pré-existente em `settings_ai_providers` (dívida documentada — sugere pgsodium). Story não introduz regressão.
- **BI-VOICE-01**: 8/8 ACs implementados. Edge fn `gemini-live-token` com JWT auth, tenant via app_metadata, rate limit 20/h, single-use 60s TTL token, audit log sem api_key. PR #12 está **CLOSED + CONFLICTING** no GitHub — devops precisa rebase + recriar antes de merge. Aprovação QA mantida.
- **BI-VOICE-02**: 9/9 ACs implementados (4 arquivos novos: useGeminiLive, gemini-live types, audioWorkletProcessor, pcm). 3 HIGH para follow-up: (1) hook não emite codes nomeados que VoiceChatButton espera (GEMINI_NOT_CONFIGURED/RATE_LIMITED/NotAllowedError) — UX cai em fallbacks frágeis; (2) estado `thinking` declarado no union mas nunca emitido; (3) `reconnectRef` declarado e nunca lido (dead code). PR #13 mergeable; sugerido BI-VOICE-02-fixup.
- **AUTH-V2-01 (PR #36)**: 2 escopos — remoção `extractTenantId` (commit `095fb0ff`) + P0 ProtectedRoute/useStepUpAuth (commit `dad9e7a1`). 5/5 ACs da story atendidos. Auditoria confirmou 0 hits de `extractTenantId` em supabase/, e 0 importadores na main pré-merge (era código morto vulnerável). `mfaChecking` fix fecha race window real de privilege escalation (children renderizavam com fetches autenticados antes do redirect MFA). `useStepUpAuth` lastGrantedAt agora `useRef` per-instance. TSC + ESLint limpos. Issue arquitetural HIGH de AUTH-V2-03c (UX gate vs security gate) **persiste** — endereçar em story de hardening cross-cutting separada.
- **BI-VOICE-02-fixup (c4908b57)**: re-review dos 3 HIGH originais, todos endereçados. (1) `mapTokenError(status, body)` adicionado: 412→GEMINI_NOT_CONFIGURED, 429→RATE_LIMITED com `retry_after_seconds`, 502→PROVIDER_ERROR; `mapMediaError` checa `err.name === 'NotAllowedError'` corretamente — VoiceChatButton.tsx:164/179/189 recebe codes esperados. `fetchToken` lê `invokeError.context.status` direto (Response object) em vez de regex sobre message. (2) `'thinking'` emitido em `turnComplete` quando `PlaybackQueue.isPlaying === false`, com fallback `setTimeout(() => 'listening')`. (3) `reconnectRef` agora invocado em `ws.onclose` via setTimeout; bonus — `isReconnectingRef` resolve race condition (MEDIUM #1 da review original) entre goAway timer e onclose. TSC+ESLint limpos. Issues prévias remanescentes não-bloqueantes: blob URL global, sem unit tests, AC3 text desatualizado.
- **AUTH-V2-03c-fixup**: 4/4 ACs atendidos. Migration `20260426002000_mfa_recovery_consume_atomic.sql` envolve UPDATE used_at + SELECT factor_id + DELETE FROM `auth.mfa_factors` em transação Postgres única (SECURITY DEFINER + search_path inclui `auth`). Rollback transacional via `RAISE EXCEPTION 'factor_not_found'` quando factor ausente — código permanece reusable. `MfaVerify.tsx` não importa mais `unenrollSelf` no recovery flow (consome resultado jsonb `{success, error?}`). `useMFA.ts:74-85` atualizado: tipo de retorno mudou para jsonb e invalidateQueries só dispara em success. Race condition "código queimado + factor preso" eliminada de forma transacional. TSC+ESLint limpos. 1 LOW: comentário em `useMFA.ts:87` diz "requires AAL1" mas Supabase exige AAL2 para unenroll — não afeta runtime, função não usada pós-fixup. Recomendado teste manual end-to-end em staging antes de prod.
- **SENDS-PRO-REVISAO**: PR composite — SENDS-IMPORT-01 (remoção CSV templates + preset selector + hook órfão deletado), SENDS-IMPORT-02 (26 Q-fields + company struct + edge fn) e 9 fixes do relatório do delta (P1-01 join sends_webhooks, P1-02 validação phone `^\d{6,15}$`, P1-04 fallback person null em useSendContacts, P1-05 has_more pré-dedup, P2-02 timezone scheduled_at, P2-05 filtros UTM, P2-09 LiveCounter active, P3-03 timeout webhook, P3-04 search por nome) + P2-04 aceito por justificativa. TSC limpo; nenhuma regressão. Débito declarado fora do escopo: P1-03 (atomic CAS sends-dispatch-batch — story follow-up nova) e P2-07 (leads para existing people — story follow-up). Issues remanescentes não-bloqueantes para próxima rodada (SENDS-FIX-02 ou similar): P2-01, P2-03, P2-06, P2-08, P3-01, P3-02. Cosméticos: 5 erros `any` em arquivos do escopo + commit 39b0d013 com mensagem incompleta. Lead aprovou implícito a remoção do preset selector via gate final. Veredicto detalhado em `verdict-sends-pro-revisao.md`.
- **FIX-IG-AUT-01**: 6/6 ACs atendidos (AC1/AC2/AC3 via deep-dive manual do dev-architect; AC4/AC5/AC6 via código). Root cause confirmado: `meta-pages-subscribe/index.ts:91` assinava apenas `'leadgen'` enquanto `meta-save-credentials` assinava o conjunto completo — tenants configurados via UI ADM nunca recebiam webhook de `comments`. Fix expande `subscribed_fields` para `'messages,comments,leadgen,messaging_postbacks,feed'`. AC6 adiciona `logNoMatch()` em dois early returns silenciosos do runner (`no_active_automations`, `no_post_match`) — agora registra em `instagram_automation_log` com `status='no_match'` e detail JSON em `error_message`. Migration `fwup28` segue padrão idempotente DROP+ADD CONSTRAINT (igual fwup25/fwup26). Front sincronizado: tipo TS expandido, STATUS_CONFIG com ícone laranja AlertTriangle. Mudanças aditivas — zero regressão. CONCERN-OPS: tenants pre-existentes precisam ser re-subscritos manualmente (fix de código não retroage no Meta Graph); devops deve rodar job ou comunicar. CONCERN-TEST: sem suite cobrindo handler de comments nem logNoMatch — validação será manual em tenant real.
- **BI-VOICE-04 (PR #39)**: 7 ACs estáticos atendidos (AC1, AC3, AC5, AC6, AC8, AC9, AC10); AC2/AC4/AC7 são smoke tests deferidos para staging. AC1: VoiceChatButton.tsx:378-381 passa `onToolCall: useCallback(call => executeBiTool(call.name, call.args, supabase, settings?.id))` ao useGeminiLive. AC3: biTools.ts:125-129 aceita `tenantId?` por DI; logToolInvocation fallback SELECT em settings + early-return se nulo + INSERT inclui tenant_id correto (linha 214). AC5: BiVoiceFeatureToggle importado e montado em AIProvidersConfig.tsx:30,335. AC6: migration `20260426003000_settings_bi_voice_beta_role_guard.sql` substitui blanket `authenticated_write` por split policies; UPDATE com WITH CHECK que aceita se flag não muda OR caller é gestor/super_adm via `check_bi_voice_beta_update()` SECURITY DEFINER — frontend e SQL alinhados via mapeamento `useAuth.ts:173-176`. Rollback existe + manifest entry. AC8: Dialog com texto "Cada minuto de conversa por voz tem custo de uso da Gemini API (per-tenant)" + bullets reforçando audit log e desabilitar quando não em uso. TSC limpo. 3 LOW: smoke tests AC2/AC4/AC7 deferidos; menção a audit log no UI sem INSERT visível em handleConfirmEnable (pode ser via trigger); `settings_users.active=true` no helper revoga acesso de user desativado (intencional, mas vale documentar). Devops liberado para merge.
- **SENDS-IMPORT-02 (re-verificação 2026-04-30)**: 11/12 ACs atendidos + 1 parcial (AC8). Implementação verificada arquivo por arquivo: `qFieldLabels.ts` com 26 entradas + aliases; FieldMapper.tsx adiciona grupos "Qualificação (Q-fields B2B)" e "Dados de Empresa", remove `company` de crmOptions/HEURISTICS; useImportarLista.ts:6-17 com `q_field?` e `company_struct?`, sem `company?`; ImportPreviewTable.tsx renderiza colunas dinâmicas; edge function `sends-import-contacts/index.ts` extrai Q-fields, faz UPDATE non-empty-only para pessoa existente, INSERT com spread para nova, dedup company por tax_id→trade_name normalizado, upsert clients_people_companies com is_primary calculado. AC8 ficou parcialmente literal: tipo `field_mapping.company?: string` permanece em ImportContactsInput (linha 38) por design defensivo — warning é logado se chega payload legado. Aceito como CONCERN não-bloqueante. Issues LOW: rótulo "Nome Fantasia" no FieldMapper.tsx:64 ao invés de "Nome da empresa" sugerido; alias 'documento' colide entre FIELD_KEY_ALIASES.cpf (linha 45) e COMPANY_KEY_ALIASES.tax_id (linha 56), provoca ambiguidade dependendo da ordem do loop; AC11 smoke test (CSV de 5 linhas) não verificável estaticamente. TSC zero erros nos arquivos do escopo. Story status atualizado backlog→done.
- **FIX-SENDS-01 (re-verificação 2026-04-30)**: 5/5 ACs atendidos. `sends-dispatch-batch/index.ts` existe e implementa loop server-side com auth dual-mode (service role + JWT), enforça cadência via `last_batch_at + send_interval_seconds`, marca timestamp ANTES da invocação para prevenir double-dispatch concorrente, usa `AbortSignal.timeout(55000)` para evitar lock no minuto seguinte, marca `status='completed'` quando worker reporta `has_more=false`. Migration `20260423010000_sends_server_dispatch.sql` cria `last_batch_at` + `trigger_sends_dispatch_batch()` SECURITY DEFINER lendo `_app_config` + `cron.schedule('sends-dispatch-batch', '* * * * *', ...)`. `useSendDispatch.ts:59-92` dispara primeiro batch via send-dispatch-worker e entrega ao pg_cron; setInterval em LiveCounterSidebar.tsx:43 é animação UI counter (não-relacionado). LOW issues: (1) migration `20260423013000_sends_server_dispatch.sql` é DUPLICATA EXATA bit-by-bit (diff vazio) da `20260423010000` — idempotência salva (CREATE OR REPLACE + IF NOT EXISTS + cron.schedule re-run), mas suja histórico; recomendado cleanup. (2) `sends-dispatch-batch` sem entrada em `supabase/config.toml` — verify_jwt=true default aceita service-role token, mas explicitar para clareza ops. (3) AC4 smoke test (broadcast continua após fechar aba) deferido para staging. Story status atualizado backlog→done.
- **FIX-IG-OUTBOUND-IGA-ROUTING (commit 987057b3)**: PASS. `instagram-outbound/index.ts` adiciona `isIgaToken()` que detecta tokens IGA pelos prefixos `IGAF`, `IGQVJx`, `IGQVJt` (3 prefixos justificados — formatos curtos e long-lived da Meta) e roteia: IGA → `graph.instagram.com/v25.0/{igBusinessId}/messages` (single attempt, sem page token); EAA/Page → `graph.facebook.com/v25.0` mantendo fluxo antigo (page token exchange + igbid fallback). `getPageAccessToken` retorna early `null` para IGA (skip de exchange unsupported). Per-attempt baseUrl em `attempts: Array<{senderId, token, baseUrl}>`. Helper `isIgaToken` bem encapsulado, sem callers externos. Comentários inline e header doc atualizados. Único caller (omni-delivery-engine:387) apenas delega — sem mudança necessária. TSC limpo. Sem regressão para tenants EAA: ramo `if (!tokenIsIga && pageId && pageToken)` preservado com mesma assinatura. Cobertura cross-channel: meta-inbound discrimina por `object` (instagram vs whatsapp_business_account), então fix em outbound não afeta WhatsApp inbound/outbound nem comments handler.
- **FIX-OMNI-DIRECTQUERY-IG-TIKTOK (commit 857a6b0e)**: CONCERNS. `useConversasSimples.ts:54` corrige fallback directQuery: trocou `.not('whatsapp', 'is', null)` por `.or('whatsapp.not.is.null,instagram_id.not.is.null,tiktok_open_id.not.is.null')` — agora contatos IG-only e TikTok-only retornam corretamente quando RPC `get_omni_contacts` indisponível (path normal em tenants sem fwup27). Coluna `tiktok_open_id` confirmada via migration `20260413230000_tiktok_integration.sql` (e idempotente em `20260430150000_fwup27_clients_people_missing_columns.sql`). `Conversas.tsx:680` corrige reset `clearFiltrosSecundarios`: agora `new Set(['whatsapp', 'instagram', 'email', 'sms', 'telefone', 'tiktok'])` — consistente com initial state em :230. Issue LOW pré-existente NÃO introduzida por este fix: `filtroCanais.size < 5` em :670 (badge "algum canal desmarcado") está desatualizado vs `>= 6` em :406 (early return "todos marcados") — bug latente desde commit `9777202c8` (2026-02-20). Com 6 canais agora, badge sempre `false` quando 5-6 marcados; só aparece com ≤4. Recomendado fix de hardening cross-cutting em outra rodada — não bloqueante. TSC ok. Lint ESLint mostra 28 erros `any`+ `@ts-nocheck` em Conversas.tsx — TODOS pré-existentes em código não tocado pelo fix.
- **FIX-IG-AUTOMATION-IDEMPOTENCY (commit 3524ce71)**: FAIL. Bug crítico no fluxo do `SkippedError`. A classe é lançada nas linhas 325 (comment_reply path) e 449 (DM comment_recipient path) ao detectar Meta `error_subcode 2534023`, MAS é capturada pelos try/catch LOCAIS (L351 e L531) que apenas chamam `errors.push("comment_reply: " + msg)` ou `errors.push("send_dm: " + msg)` — perdem a identidade de classe. No final de `executeAction` (L544-546): `if (errors.length > 0 && (dmFailed || !commentOk)) throw new Error(errors.join(' | '))` — re-lançam **`Error` genérico**. No loop principal (L162-169), `e instanceof SkippedError` é sempre `false` → status cai em `'failed'`, NÃO em `'skipped'`. Resultado: o cenário central do commit ("comentário que já tem resposta → Meta retorna 2534023 → deve registrar `skipped`, não `failed`") **continua quebrado**. Idempotency check (L128-140) está correto e funciona — esse cobre cenário "mesmo webhook duplicado", que JÁ retorna skipped corretamente porque ocorre antes de `executeAction`. Schema da tabela suporta valor 'skipped' (CHECK constraint inclui — migration `20260311100000_instagram_automations.sql:42-43`). **Fix necessário:** ou (a) re-lançar SkippedError preservando classe nos catch locais (`if (e instanceof SkippedError) throw e;` antes do push), ou (b) substituir o `throw new Error(...)` final por uma checagem de tipos preservando SkippedError quando todos os errors push são originados dela. TSC limpo; problema é puramente lógico de fluxo de erro.
- **FIX-IG-INLINE-ERROR-DISPLAY (2026-04-30)**: PASS. `useConversas.ts` adiciona callback opcional `onIgDeliveryError?: (msg: string) => void` em 3 paths de erro: invoke error (L412-417), failed result (L418-423), network catch (L425-428). Toast original preservado, callback é adição cumulativa. `Conversas.tsx`: estado `sendError` (L279) + useEffect cleanup ao trocar pessoa/canal (L544) + callback passado APENAS quando `canalAtivo === 'instagram'` (L928, não vaza para WA) + banner inline persistente com XIcon (já importado em :7) e paleta destructive consistente. TSC limpo. Sem regressão.
- **FIX-ORA-EMPTY-INBOX-FWUP30 (2026-04-30, re-revisão pós-fix Alpha)**: CONCERNS (não-bloqueante — workaround intencional). Migration `20260430180000_fwup30_get_omni_contacts_rpc_v2.sql` corrige RPC: (1) canal filter inclui `tiktok_open_id` (L31); (2) status filter trata NULL como 'open' via `(p_status_atend = 'open' AND (p.service_status = 'open' OR p.service_status IS NULL))` (L42-46). Entrada `client-migrations.json:967-968` adicionada com order_index 10184. `useConversasSimples.ts` directQuery (L68-71) está correto: `'open'` → `.or('service_status.eq.open,service_status.is.null')`, outros status → `.eq()`. **Fix adicional do Alpha em `tryRpc` (L26):** quando `statusAtendimento === 'open'`, passa `null` em vez de `'open'` para a RPC. Justificativa: enquanto fwup28 (em prod) só matcha exato (`service_status = 'open'`, exclui NULL), passar `null` faz a RPC bypass o filtro de status e retornar contatos NULL imediatamente — resolve sintoma central (inbox João Guirunas vazia) sem aguardar propagação fwup30. Trade-off: bypass também retorna `closed`/`pending`. Para João Guirunas (maioria NULL, closed minoritário em tenants legados) é aceitável; para tenants já migrados via p10 com closed real, pode causar mistura visual quando default 'open' está ativo (Conversas.tsx:224). **Follow-up obrigatório pós-fwup30 propagada:** reverter `tryRpc` para enviar `'open'` literal e remover bypass — fwup30 já trata NULL como 'open' server-side corretamente. **Observação opcional fora-do-escopo:** existe migration `fwup29_reactivate_instagram_token_refresh_cron.sql` no working tree mas SEM entrada em `client-migrations.json` — não bloqueia este gate, mas devops deve verificar se reativação do cron é intencional para tenants antes do push.
- **FIX-SENDS-IMPORT-PIPELINE-DUPLICADO (commit de11ae20)**: PASS. Regressão confirmada: FIX-SENDS-IMPORT-06 adicionou pipeline+etapa dentro de `ImportListaTab.tsx` (L36-38, 87-89, 138-176 — `createLeads`/`pipelineId`/`stageId` próprios passados ao edge fn `sends-import-contacts`), enquanto `CriarDisparo.tsx` mantinha bloco "Destino dos leads novos" no sidebar para `importMode === 'imported'` — usuário via dois seletores. Fix remove o bloco JSX (antes L565-601), os 3 estados (`importCreateLeads`/`importPipelineId`/`importStageId`), e a derivada `importDestStages`. Ternary `importMode === 'filtered' ? ... : ...` virou condicional `&&` corretamente. Payload do send para modo imported agora envia `pipeline_id: null` e `stage_ids: null` (correto: lead já entra no pipeline via import — duplicar no send seria bug). Modo `filtered` ("Mover leads após envio") preservado intacto. Zero referências órfãs aos estados removidos em `CriarDisparo.tsx`. `LpPro.tsx` tem `importPipelineId/importStageId` próprios mas escopo distinto (Landing Page Pro), não afetado. TSC limpo. Sem regressão.
- **FIX-IG-AUTOMATION-IDEMPOTENCY (re-submetido commit f96a7920)**: PASS. Fix mínimo cirúrgico: adiciona `if (e instanceof SkippedError) throw e;` como primeira linha dos dois catch locais (L352 comment_reply, L533 send_dm). Ambos preservam a classe e re-lançam, fazendo o erro chegar intacto ao try/catch do loop principal (L162-170) onde `instanceof SkippedError` agora é true → `status='skipped'` é atribuído corretamente. Verifiquei 9 cenários do fluxo de erro: (1) reply_comment + 2534023 → skipped ✅; (2) send_dm comment_recipient + 2534023 → skipped ✅; (3) reply_and_dm misto comment-ok + DM-erro-genérico → failed correto ✅; (4) reply_and_dm + 2534023 no comment → skipped, DM nem é tentado (semântica intencional do Rex: se comment já foi respondido, automação anterior já completou) ✅; (5) reply_and_dm + comment-ok + DM 2534023 → skipped (mensagem do comment foi gravada em messages, log marca skipped — consistente) ✅; (6) idempotency check L128-140 não muda → skipped 'idempotent_comment' ✅; (7) sucesso normal → success ✅; (8) erro genérico em comment_reply puro → failed via L546+L547 throw new Error ✅; (9) send_dm puro com erro genérico → failed ✅. Schema `instagram_automation_log` aceita 'skipped' (CHECK migration `20260311100000:42-43`). TSC limpo. Sem regressão.
- **FIX-SENDS-TEMPLATE-VARS (commit 04926eaa)**: CONCERNS (não-bloqueante). Diff em `send-dispatch-worker/index.ts:957-967`: remove guard `if (val) customFields[pos] = val` — agora `customFields[pos] = val` sempre executa quando `variables_map` cobre a posição, mesmo com `val === ''`. Confirmado fluxo: (a) `customFields` populado de `lead_field_values` (L941-952); (b) loop sobre `variables_map` mapeia posição→key e atribui `''` quando key inexistente; (c) `personForRender.customFields` (L970) → `resolveTemplateVar` (L35-48) faz `...person.customFields` ÚLTIMO no spread, sobrescrevendo hardcoded `'2': person.whatsapp` com `''`. Bug confirmado para templates João Guirunas: 23 templates em `supabase/tools/ora_setup_templates_followups.sql` têm `variables_map` mapeando `'2'`→`recomendante`/`relacao_erika`/`nome_evento` etc. Quando lead sem campo populado, posição 2 caía no fallback `person.whatsapp` — telefone aparecia no texto. Fix elimina leak. **CONCERN-1 (LOW):** templates legados SEM `variables_map` (e.g. inseridos antes de João Guirunas — `20251022161830_38d44cd9...sql`) caem no early-return da branch `if (variablesMap)` e mantêm comportamento antigo (hardcoded {1}=name, {2}=phone, {3}=email). Não é regressão, é status quo. **CONCERN-2 (LOW):** templates com `variables_map` parcial (cobre só `'1'` mas template tem `{{2}}` no body) ainda renderizam telefone na posição não mapeada — fix não previne esse cenário, requer template ter mapa exaustivo. Para João Guirunas todos os 23 templates já têm mapas exaustivos (verificado via grep). TSC limpo (no-scope); types pré-existentes não regrediram. Sem mudança em config.toml necessária. Sem regressão para WA outbound (omni-delivery-engine ou whatsapp-outbound) — fix isolado em send-dispatch-worker.
- **FIX-TLDV-SAVE (commit 04926eaa)**: CONCERNS (não-bloqueante). Diff em `TldvIntegrationConfig.tsx:142`: adiciona `display_name: 'tl;dv'` ao payload do upsert — satisfaz constraint NOT NULL em `omni_channel_configs.display_name` (definida em `20260308010001_omni_channel_configs-ok.sql:11` e `20260312150000_ensure_full_tenant_baseline.sql:931`). Bug confirmado: tenants onde a migration `20260421180000_tldv_integration.sql` (que faz INSERT da row tldv via seed L30-32) NÃO foi propagada teriam `omni_channel_configs` sem linha tldv; o upsert do componente, restrito por `onConflict: 'channel'`, tentaria fazer INSERT com apenas `{channel, credentials}` e falharia por violação de NOT NULL. Fix supre o campo no payload. **Sem regressão em outras integrações:** upsert tem filtro implícito `channel: 'tldv'` + onConflict='channel' — só toca a linha tldv. Outros canais (whatsapp, instagram, email, sms, telefone, identity_collection) não afetados. **CONCERN-1 (LOW):** inconsistência nominal — seed da migration insere `display_name='tldv'`, código usa `'tl;dv'`. Tenants com seed prévia mantém valor antigo no INSERT, mas em saves subsequentes o upsert sobrescreve para `'tl;dv'` (porque payload do `.upsert` é completo, não merge). Cosmético, não bloqueia funcionamento. **CONCERN-2 (LOW):** seed migration usa `COALESCE(omni_channel_configs.display_name, 'tldv')` no ON CONFLICT — preserva valor existente; o upsert do componente NÃO usa COALESCE — perde display_name customizado se admin tiver editado manualmente. Improvável em produção (UI não expõe edit), mas vale registrar. TSC limpo (no-scope; erro TS2769 em :141 é pré-existente do schema do supabase client). Sem regressão.
- **FIX-AGENTES-PRO-SAVE (commit 4fa57674)**: PASS. Fix em três camadas defensivas. **Camada 1 (client):** helper `uuid()` em `useAgentesIAReal.ts:398-399` normaliza `string | null | undefined` → `null` quando vazio/whitespace. Aplicado em 4 campos UUID opcionais antes do RPC: `pipeline_id` (L411), `leads_stages_id` (L414), `llm_provider_id` (L424), `wa_channel_id` (L427). **Camada 2 (RPC SQL):** migration `20260430260000` substitui `(p_agent_data->>'pipeline_id')::uuid` por `NULLIF(p_agent_data->>'pipeline_id', '')::uuid` em todos os 4 campos (L67-98). CREATE OR REPLACE FUNCTION + SECURITY DEFINER + SET search_path = public preservados. **Camada 3 (state guard):** `AgenteSingle.tsx:53-77` adiciona `if (!salvarMutation.isPending)` em 2 useEffects de sync — evita que `agente`/`passos` refrescados sobrescrevam edits locais durante save em voo (race entre TanStack Query refetch e mutation in-flight). **Migração preserva fix anterior (10190 humanizacao):** linha 100 `humanizacao = COALESCE((p_agent_data->>'humanizacao'), humanizacao)` SEM cast `::boolean` (correto: humanizacao é text 'alta'/'media'/'nenhuma'). **Manifest entry adicionada** em `client-migrations.json:1001-1005` com order_index 10192. **Sem dados duplicados:** RPC faz `DELETE FROM ai_agents_steps WHERE ai_agent_id = p_agent_id` antes de re-inserir (L118-119), tudo dentro de `SELECT ... FOR UPDATE` em `ai_agents` no início (L26-31) — locking transacional previne race. **Sem race condition:** dois saves concorrentes serializam pelo lock; `current_version + 1` lido sob lock garante incremento monotônico. **Sem regressão:** TSC limpo no escopo; erros TS pré-existentes (linhas 462, 593, 687/688) não relacionados ao fix. AgenteSingle linhas 65/66 que aparecem como erro novo são pré-existentes (deslocaram por inserção de useEffect). Migration idempotente. Defesa em profundidade — protege independente de quem chame a RPC (UI, scripts admin, MCP).
- **USER-TYPES-REFACTOR (2026-05-02)**: CONCERNS (4 não-bloqueantes; veredicto inicial PASS revisado para CONCERNS após auditoria adicional do lead identificar regressão latente em getTeamFilter). Refatoração 5 tipos legados (`gestor|consultor|atendente|cliente|gerente`) → 3 canônicos (`admin|manager|user`). 8/8 critérios do checklist verificados. **Item 1 (remoção tipos antigos):** zero hits funcionais como `user_type` em src/ e supabase/functions/. Hits remanescentes (`'cliente'` em from_contact/from_message; `'consultor'` em sort fields; vocab em mensagens de UI/comentários; regex de prospect-search) são domain-specific NÃO relacionados ao enum `user_type`. **Item 2 (DB):** migration `20260502120000_user_types_canonical_refactor.sql` em BEGIN/COMMIT, ordem correta (drop constraint → backfill `super_admin→admin` precedence → nova constraint `['admin','manager','user']` → default 'user' → `is_admin_or_manager()` atualizada → `create_tenant_user` default 'user' → DO block dinâmico recria ~40 RLS policies via REPLACE em pg_policies preservando permissive/roles/cmd/USING/WITH CHECK). Rollback fiel disponível em `rollbacks/20260502120000_…rollback.sql` (mapeamento aproximado documentado). `client-migrations.json` order_index 10200 (último, sem colisão). `schema_baseline.sql:2447` constraint atualizada; ~60 ocorrências de policies regeneradas para `user_type = ANY (ARRAY['admin','manager'])`. **Item 3 (edge functions):** 10 edge fns auditadas — `delete-user`, `create-tenant-user`, `create-global-user`, `admin-unenroll-mfa`, `score-re-evaluate`, `update-user-password`, `update-user-email`, `filter-leads-for-send`, `bi-sync-meta-ads`, `bi-sync-google-ads` — todas usando `user_type === 'admin'` e `user_type === 'manager'` como autorização. `create-global-user:31` valida via `z.enum(['admin','manager','user'])`. `adm-create-user:154` define `user_type:'admin'` ao criar global admin. **Zero comparações contra valores legados** como autorização. **Item 4 (frontend hooks):** `types/usuarios.ts:1` exporta `UserType = 'admin'|'manager'|'user'`. `useAuth.ts` profile interface tipada (L63), fetchUserProfile (L197/200) deriva `gestor` (manager OR admin) e `super_adm` (admin) corretamente do `user_type` do banco. `useUserPermissions.ts` expõe `isAdmin/isManagerStrict/isUser` precisos + `isManager/isGestor/isSuperAdmin` agregados (semântica histórica preservada para callers legados). `canDeleteUser` exige admin estrito (correto). Permission flags coerentes com matriz da especificação. `useTeamMembers/useTimes/useAtribuicaoNegocio` derivam `gestor` flag por `user_type IN ('admin','manager')` consistentemente. `useUsersNew.ts:183-187` mapeia campo legado `gestor` → `user_type='manager'` em mutation (backward-compat para callers antigos, intencional). **Item 5 (UI):** `NovoUsuarioModal.tsx:163-184` RadioGroup com 3 opções exatas (User/Manager/Admin) + descrição contextual. `EditarUsuarioModal.tsx:280-301` idem + fallback inteligente para usuários antigos sem `user_type` (L51-54: usa `super_adm` flag). `UsuariosConfig.tsx:57-64` `resolveUserType` faz coalesce DB→legacy flags. Badges renderizam Admin (purple ShieldCheck) / Manager (blue Shield) / User (emerald UserCircle). Zero opções "Atendente"/"Consultor"/"Gestor" como valor de RadioGroup funcional. **Item 6 (type safety):** zero casts contra valores legados. `'user' as UserType` em useState inicial (trivial, OK). **Validação automatizada:** `npx tsc --noEmit` clean (zero erros). ESLint reporta 858 issues totais — auditoria filtrada por arquivos do escopo: 2 `any` pré-existentes em useAuth.ts:289 (`authListener: any`) e useTimes.ts:21 (`as any[]`); 1 `any` novo em useUserPermissions.ts:65 → CONCERN-2 LOW. **CONCERN-1 LOW:** `PermissoesConfig.tsx:215,219` compara `role.name === 'gestor'` em sistema RBAC `tenant_roles` separado de `user_type`. Se a tabela `tenant_roles` ainda mantém uma row de sistema com `name='gestor'`, isso é referência ao role custom legado — não bloqueia este escopo (refatoração é sobre `settings_users.user_type`), mas seria limpeza coerente. **CONCERN-2 LOW:** `useUserPermissions.ts:65` `(user.profile as any).role_id` — `role_id` não declarado na interface profile do `useAuth.ts:52-71`; trade-off entre cast loose vs expandir interface; cosmético. **CONCERN-3 INFO:** `useUsersNew.ts:183-187` aceita campo legado `gestor` em updates por backward-compat; documentado com comentário inline; remoção possível em sprint futuro após auditoria de callers. **CONCERN-4 LOW (regressão funcional latente, identificada em re-auditoria pós veredicto inicial):** `useUserPermissions.ts:124-128` `getTeamFilter()` perdeu branch original `if (isConsultor && userTimes.length > 0) return userTimes` durante simplificação (já que `isConsultor` virou constante `false` na refatoração). Caller `Conversas.tsx:352-358` faz `if (userTeam && filtroTimeVendas === 'todos') setFiltroTimeVendas(userTeam)` — agora userTeam sempre `""` para tipo `user`, auto-filtro de time não aplica. NÃO é blocker imediato porque `isReunioesModuleActive=false` está hardcoded em Conversas.tsx:313 (módulo Reuniões inativo no João Guirunas atual). Recomendação: restaurar branch usando `basePermissions.isUser && userTimes.length > 0` antes de reativar módulo Reuniões. **Sem regressões ATIVAS detectadas:** RLS preserva semântica (admin+manager veem tudo via is_admin_or_manager); `'user'` (atendente legado) mantém escopo restrito via filterFunctions/canManage falsy. **Próximo passo:** dev-devops liberado para iniciar USER-06 (commit + PR). Re-check git state imediatamente antes do veredicto: 26 arquivos modificados + 3 não monitorados, todos coerentes com o escopo da refatoração.

- **ora-agents-pro-fix (2026-05-02)**: PASS. Working tree: `src/pages/AgenteSingle.tsx` (+163/-0, ramo voice adicionado antes do `return` do ramo texto — texto literalmente intacto), `src/components/agentes-ia/VoiceAgentConfigTab.tsx` (novo, 634 linhas), `supabase/functions/elevenlabs-agent-sync/index.ts` (4 fixes). **AgenteSingle ramo voice (L260-419):** branch `if ((editedAgent.agent_type ?? agente.agent_type) === 'voice')` — header com badge "Voz" purple + badge sync status dinâmico (synced/pending/error/not_applicable mapeados via switch L262-273) + botão "Sincronizar" proeminente (`bg-purple-600`) + Excluir + Salvar; tabs reduzidas a "Configuração" (→ VoiceAgentConfigTab) e "Versões" (→ HistoricoTab existente reusado). **VoiceAgentConfigTab.tsx:** props `Partial<AgenteIA>` + `onAgentChange` + `onSync` + `isSyncing` (linhas 146-151), todos campos acessados existem em `AgenteIA` (`useAgentesIAReal.ts:37-49`: agent_type, voice_enabled, voice_id, elevenlabs_agent_id, voice_first_message, voice_language, voice_model_id, voice_stability, voice_similarity, voice_speed, el_sync_status, el_last_synced_at). Hooks `useElevenLabsVoices`/`useElevenLabsAgents` existem (`useElevenLabsConfig.ts:131,180`); tipos exportados (`ElevenLabsVoice`, `ElevenLabsAgent`) cobrem todas as props acessadas. Resolve `elStringId` via lookup em `elAgents` por UUID FK (L167-171) — correto dado o modelo `ai_agents.elevenlabs_agent_id (UUID FK) → elevenlabs_agents.id` e o ID de string EL real em `elevenlabs_agents.elevenlabs_agent_id`. Sliders com defaults (0.5/0.75/1.0) e cláusulas seguras (`?? value`). **Edge fn 4 fixes (`supabase/functions/elevenlabs-agent-sync/index.ts`):** (1) L106-113 `voice_id` null retorna 400 com mensagem clara "voice_id é obrigatório — defina uma voz no agente ou configure uma voz padrão em Configurações > ElevenLabs"; (2) L128 fallback `agent.voice_model_id || config.default_model_id || 'eleven_flash_v2_5'`; (3) L210-222 upsert `elevenlabs_agents` agora popula `language, first_message, llm_provider, llm_model, status='active'` além de campos antigos; (4) L230-232 `console.error` em `upsertErr`. PATCH/POST condicional preservado, retry/error handling intacto, schema retorno `{ success, elevenlabs_agent_id, sync_status }` inalterado. `tsc --noEmit` zero erros; `eslint` zero issues nos arquivos modificados. **Sem regressão no ramo texto agents:** branch voice retorna ANTES do `return` original; ramo `agent_type !== 'voice'` continua renderizando ConfiguracaoTab + ContextoTab + PromptsTab + HistoricoTab + ExecucoesTab + AnalyticsTab idênticos. Save handler `handleSave` (L126-189) persiste todos os 11 campos voice e dispara `handleSync()` automaticamente após save quando voice (L182-184) — comportamento esperado. **CONCERN-1 (LOW-OPS, declarado pelo lead):** edge fn `elevenlabs-agent-sync` ainda não deployada — sem deploy, fixes inertes (botão "Sincronizar" continuará crashando com `voice_id` null). **CONCERN-2 (LOW-OBS):** edge fn sem entrada em `supabase/config.toml` (28 entries existem para outras fns); status quo pré-fix, não introduzido por esta mudança. **CONCERN-3 (LOW-UX):** `VoiceAgentConfigTab.tsx:339-348` `new Audio(selectedVoice.preview_url!).play()` sem controle stop/cleanup — múltiplos cliques rápidos sobrepõem áudios. Cosmético, não bloqueante. Erros pré-existentes mencionados pelos devs em `useAgentesIAReal.ts` e `registry.ts` confirmados como não relacionados ao escopo (TSC limpo nos arquivos modificados).
- **ora-fix-agent-save-409 (migration 20260501130000)**: CONCERNS (não-bloqueante). Bug original: HTTP 409 / Postgres 23503 ao salvar agente em `/settings/crm/aiagents/:uuid` — FK `ai_agents_history.created_by → settings_users(id)` violada porque frontend `useAgentesIAReal.ts:471` envia `auth.users.id` em vez de `settings_users.id` (entidades distintas, ligadas via `auth_user_id UNIQUE`). Fix opção A (RPC defensivo) em `supabase/migrations/20260501130000_fix_save_agent_complete_resolve_created_by.sql`: resolve `created_by` em 3 branches — (1) já é `settings_users.id`, (2) é `auth.users.id` → lookup via `auth_user_id`, (3) fallback `auth.uid()`; senão grava NULL (FK aceita por `ON DELETE SET NULL`). Verificações estruturais: `settings_users.id` é PK, `auth_user_id` é UNIQUE (`settings_users_auth_user_id_key`) → todos lookups determinísticos (≤1 linha). UPDATE de `ai_agents` + replace de `ai_agents_steps` preservados **verbatim** da versão anterior `20260501120000_array_types` (diff confinado às linhas 38, 62-89: DECLARE + bloco resolve + INSERT). Rollback `rollbacks/20260501130000_…rollback.sql` confere bit-a-bit com 120000 — reversão limpa via BEGIN/COMMIT. Único caller no codebase: `src/hooks/useAgentesIAReal.ts:471` (frontend) — nenhum edge function chama, fix backend cobre 100%. Frontend intencionalmente NÃO muda. Sem regressão em criação (RPC é UPDATE-only com `IF NOT FOUND THEN RAISE EXCEPTION`, criação usa caminho separado), sem regressão em leitura (não toca SELECT/policies/views). SECURITY DEFINER + search_path + GRANT/REVOKE preservados. Sem vetor novo de spoofing — branch 1 só aceita IDs já existentes em settings_users; attacker no máximo apaga a própria autoria gravando NULL. Performance: até 3 SELECTs pontuais em índices PK/UNIQUE, custo desprezível. **CONCERN-1 (LOW, design intencional):** fallback silencioso para `created_by=NULL` quando todas as branches falham (caller via service_role sem JWT, user removido entre saves). Documentado pelo data-engineer no comentário linhas 14-15. Hardening futuro sugerido: `RAISE NOTICE` para observability. **CONCERN-2 (INFO):** colisão UUID v4 em branch 1 teoricamente possível, probabilidade ~zero, sem ação. **CONCERN-3 (OPS):** migration **ainda não aplicada** ao banco — Bythak aguarda Grav escolher caminho (db push, CI, ou Dashboard). Verificar se precisa entrada em `client-migrations.json` (padrão observado em fixes recentes 10184/10190/10192). Smoke tests sugeridos: (1) repro pré-fix com user auth.id ≠ settings.id → 409; (2) apply; (3) re-save → 200 + `created_by` populado com settings_users.id; (4) edge case user sem registro → success com NULL; (5) regressão UPDATE básico de `ai_agents` + 1 step. Veredicto detalhado em `agent-save-409-review.md`.
- **WEBHOOK-INCREMENT (epic 8 stories 1.1/1.2/2.1/2.2/3.2/3.3/4.2/4.3 — working tree pre-push)**: CONCERNS (8/8 ACs ✅, sem itens bloqueantes; observações documentadas). Diff: `WebhookInboundConfig.tsx` (+513), `useInboundWebhooks.ts` (tipos `CreateMode`/`InboundWebhookTriggerConfig` + normalizers defensivos), `webhook-inbound/index.ts` (switch `create_mode` + `maybeDispatchTrigger`), `crm-mapper.ts` (param opcional `forceCreateLead` em `processCrmData`/`createLead`), `wa-template-render.ts` (novo, untracked — extração de helpers de lp-submit), migração `20260510210000_webhook_inbound_increment.sql` (já aplicada; rollback fiel disponível). **Story 1.1/1.2 (UI "Como enviar"):** `WebhookPayloadPreview` renderiza curl+JSON dinâmico via `buildSamplePayload`/`buildSampleCurl` + placeholders por crm_field + dois botões copy independentes (com `disabled` quando webhook sem token via `isSaved`) + aviso "Exemplo — adicione campos abaixo para personalizar" quando mapping vazio. AC1–AC6 da 1.1 e AC1–AC5 da 1.2 batem com código. **Story 2.1/2.2 (defaults pré-config):** constante `WEBHOOK_DEFAULT_MAPPING` exportada com 3 entradas (`nome`/`whatsapp`/`email`) na ordem exigida; hidratação condicional `isEdit ? existing : DEFAULT.map(spread)` (linha 677-681) — spread garante mutabilidade total dos defaults (sem `disabled`/`readOnly`), satisfazendo invariante da 2.2 sem alterar `FieldMappingEditor`. **Story 3.2 (UI Comportamento):** `Select` com 4 opções via `CREATE_MODE_OPTIONS`, microcopy dinâmica por opção, helper `createModeRequiresStage` + `stageRequirementMet` + aviso `text-amber-500/80` inline "Etapa obrigatória para este comportamento" + `*` no label "Etapa inicial" quando requerido. ⚠ **Divergência UX spec (LOW, registrar):** spec pedia `RadioGroup` (todas 4 opções visíveis + tooltips Info), alpha entregou `Select` colapsável. Funcional, perde affordance comparativa. **Story 3.3 (edge `create_mode`):** SELECT lista `create_mode, trigger_config` (linha 348); `normalizeCreateMode` whitelist defensiva; switch bifurca em `criar`/`criar_se_nao_existir` (chama `processCrmData` com `forceCreateLead: mode === 'criar'`) vs `atualizar_etapa`/`somente_etapa` (`parseCrmBuckets` + `upsertPerson` + `upsertCompany` opcional + helper local `updateLeadStage`); validação `stage_id` retorna HTTP 400 com log `webhook_inbound.create_mode_missing_stage` antes de processar payload (linhas 389-406); `action: 'created'|'updated'|'skipped'` no response body; log `create_mode_applied` (L503). **Story 4.2 (UI Disparo automático):** `WebhookTriggerConfigPanel` com Switch + bloco colapsável quando ON; campos `Canal` (locked `whatsapp`), `Canal WhatsApp` (lista `useWhatsappChannels`, validação canais=0 com warning amber), `Template` (filtra `status ∈ {approved|active|APPROVED}`), preview body do template (truncate 140 chars), `Delay` com 5 opções fixas + microcopy dinâmica; validação `triggerRequirementMet = !triggerEnabled || (waChannelId && waTemplateId)` no `isValid`; aviso `pessoa.whatsapp` mapping quando enabled e sem field mapeado para WA; hidratação completa dos 5 states a partir de `existing.trigger_config`; payload `buildTriggerConfig()` retorna `null` quando OFF, objeto completo com `wa_variable_map: {}` quando ON. **Story 4.3 (edge dispatch):** `maybeDispatchTrigger` espelha lp-submit:1099-1240 — fetch `settings_whatsapp_channels` para `phone_number_id`, fetch `whatsapp_templates`, NAMED smart defaults (`nome`/`name`/`email`/`telefone`/`phone`/`whatsapp` mapeados a `pessoa.*` via `parseCrmBuckets`), `resolveExplicit` suporta `fixed:` e `field:` (V1 sem variable_map editor, smart defaults dominam), build de `templateComponents`+`renderedBody`+`headerText`+`templateButtons` via helpers shared, INSERT em `messages` com `source_type: 'webhook'`, `module_ref_id: webhook.id`, `metadata.delay_minutes`. Try/catch global no nível da função — qualquer falha vira `trigger_dispatched: false` + log estruturado (`no_whatsapp`, `missing_config`, `channel_not_found`, `template_not_found`, `insert_failed`, `dispatch_error`); webhook responde 200 mesmo em falha total do dispatch. Curto-circuito em `action === 'skipped'` (linha 508). **AC9/segurança (crítico verificado):** `forceCreateLead` é opt-in com default `undefined` em `processCrmData`/`createLead` (`crm-mapper.ts:403, 537`); callers `lp-submit/index.ts`, `meta-inbound/index.ts:813`, `meta-leadgen-sync/index.ts:438` **não passam o param** — fluxo legado preservado byte-a-byte. `create_mode` vem do DB (não do request externo) com whitelist defensiva. Sem nova surface de input externo no webhook-inbound. **Validação automatizada:** `npx tsc --noEmit` zero erros; `npx eslint` no escopo retorna apenas 3 warnings `react-refresh/only-export-components` em exports de constantes (`WEBHOOK_DEFAULT_MAPPING`, `buildSamplePayload`, `buildSampleCurl`) dentro de arquivo de componente — não bloqueante, mover para `WebhookInboundConfig.utils.ts` em cleanup futuro. **Tech-debt registrado (não bloqueia):** (1) `lp-submit/index.ts` ainda mantém cópias locais dos helpers WA — refactor para `_shared/wa-template-render.ts` pendente; (2) sem testes unitários para `forceCreateLead` em `createLead` — regressão silent risk; (3) Deno check não executado nesta sessão (CLI indisponível) — recomendado rodar no CI antes do push; (4) `somente_etapa` idêntico a `atualizar_etapa` na V1 (documentado em comentário linhas 461-462 do edge fn), microcopy "Não cria nada novo" sugere diferença ainda não implementada; (5) constantes em arquivo de componente disparam warnings react-refresh; (6) `WebhookRow` não exibe `create_mode`/`trigger_config.enabled` — listagem perde sinal; (7) `WhatsappTemplatePickerModal` não foi reusado (spec pedia, alpha usou `Select` simples — aceitável V1 sem variable map editor). **Smoke runtime UI:** NÃO executado (read-only nesta sessão) — recomenda-se rodar `npm run dev` e validar (a) hidratação `create`/`edit` dos 5 states de trigger + createMode; (b) colapso do painel trigger ao toggle; (c) microcopy dinâmica por opção de Comportamento; (d) curl/JSON regenerando em tempo real ao editar mapping; (e) `disabled` dos botões copy em modo `create` antes de salvar. **Próximo passo:** dev-devops liberado para push com observações documentadas. Veredicto preenchido na seção QA Results de cada story em `docs/smart-memory/stories/backlog/` e `docs/smart-memory/stories/done/` (3.3, 4.3).
- **SQUAD-C-PROD-VALIDATIONS rev 1.1 (2026-06-08, pre-exec gate review)**: PASS. Doc `docs/smart-memory/ops/squad-c-prod-validations.md` (rev 1.1, owner squad-c-data-1) pronto para execução pelo dev-data-engineer contra prod. **Validação independente do QA via grep direto no arquivo:** §1.2 array contém 34 Tier 1 únicos (matches 1:1 com consolidado v2.1 §1.1) + 5 Tier 1.b doc-only = **39 tabelas SAFE_DROP**; zero references remanescentes a "33" ou "38"; `llm_connections` aparece 7× ao longo do doc (§1.2, §2.2, §2.3, comentários, changelog), confirmando inclusão completa. **Mea culpa registrado:** apontei 3 tabelas faltando no rev 1.0 (`llm_connections`, `n8n_chat_histories`, `prospect_people_v2`); apenas `llm_connections` era falso negativo real — as outras 2 já estavam no rev 1.0, erro de scope do meu grep inicial. data-1 corrigiu o gap real + documentou claim no changelog inline. **Cobertura por gate:** (1) Fase 1 — `pg_stat_user_tables` §1.2 com verdict heurístico `GO_DROP/BLOCK_DROP/MISSING_IN_PROD` baseado em `n_live_tup = 0 + last_seq_scan/last_idx_scan < now() - 30d` OU `seq_scan = idx_scan = 0 + autovacuum/analyze stale > 30d` — exatamente o gate exigido no veredicto v2.1; (2) Fase 1 — `pg_policies` §2.2 audit + §2.3 cross-table via ILIKE em `qual`/`with_check` cobre policies de terceiros que sobreviveriam ao CASCADE como dead code (subset escolhido para §2.3 inclui canônicas LLM/users/campaigns/prospect — correto); (3) Fase 2 — `pg_trigger` §3.2 com `pg_get_triggerdef`+`pg_get_functiondef` inline + §3.3 busca textual em `pg_proc` por `%contact_attempts%` em schemas public/storage/auth — resolve LOW-2 do meu veredicto v2.1; (4) Fase 3 — `pg_depend` §4.2 captura views/constraints/rules formais; §4.3 cobre o gap (funções PL/pgSQL são strings opacas ao `pg_depend`) com regex word-boundary `\m<col>\M` que evita falso positivo `old_pipeline_id` vs `pipeline_id`; §4.4 `pg_publication_tables` defensivo para replicação lógica; §4.6 caveat Squad B (`messages.thread_id`/`tokens_qty`) preservado. **Segurança:** SELECT-only validado por inspeção — zero CREATE/ALTER/DROP/INSERT/UPDATE/DELETE no doc inteiro. §6 recomenda `service_role` (não `anon` por RLS) e rodar §4.3 fora de pico (regex em corpo de função é O(n_funcs × n_cols)). **Anexos por PR:** §1.4 define `prod-validations/fase1-pg-stat-user-tables-{YYYY-MM-DD}.csv` + análogos para §2/§3/§4 (7 artefatos obrigatórios listados em §5 tabela). **Issues bloqueantes: nenhum.** **Issues não-bloqueantes:** nenhum novo — LOW-2 (pg_trigger query) e LOW-3 (canônica usuários para Fase 4) do veredicto v2.1 do consolidado continuam herdados; LOW-2 endereçado parcialmente em §3.3 (data-1 forneceu a query SQL exata). **Próximos passos:** @team-lead aprovar execução; @dev-data-engineer executar §1+§2 (Fase 1 gate), anexar CSVs nomeados conforme §1.4/§2.5, retornar resultados em `prod-validations/` para QA review pré-merge. @dev-devops aguardar gates Fase 1 PASS antes de abrir migration. Veredicto formal apenso em `docs/smart-memory/ops/squad-c-prod-validations.md` ficará registrado via Squad-C-QA quando data-1 commitar.
- **SQUAD-C-DB-AUDIT v2.1 (2026-06-08, re-QA incremental)**: PASS (limpo). Squad-c-architect entregou revision 2.1 do consolidado resolvendo o último LOW remanescente do PASS condicional v2 e reconciliando 2 tabelas Tier 4 → Tier 1 com cross-ref expandido. **Mudanças validadas independentemente pelo QA via grep em `src/**` + `supabase/functions/**`:** (1) `sistema_buffer_agente` 0 matches → Tier 1 SAFE_DROP confirmado; (2) `sistema_controle_agendamentos` 0 matches → Tier 1 SAFE_DROP confirmado; (3) **LOW-1 resolvido:** `llm_connections` 0 matches (excluindo `crm_llm_connections` e `settings_ai_providers` canônica) → binarizado §1.4 "validar tabela ativa antes" → Tier 1 SAFE_DROP em §1.1. **Métricas atualizadas:** Fase 1 sobe de 31 → 34 tabelas (Tier 1) + 5 doc-only (Tier 1.b) = **39 tabelas SAFE_DROP** total. Tier 4 INVESTIGATE cai de 8 → **6 itens legítimos** (lgpd_anonymization_log, crm_basesconhecimento*, _meta_debug, process_*/processes, conversion_*, adm_sync_jobs, bi_sdr_targets). **Caveat preservado nas 3 promoções:** mesmo gate `pg_stat_user_tables` (zero tráfego ≥ 30 dias) dos demais Tier 1 continua obrigatório — grep de código não substitui telemetria de produção. **Re-validação spot-check 7 CRITICAL v1:** score_incomes (JOIN ativo em ScoreInformationDisplay), q1_main_bottleneck/q26_disc_analysis (used em ai-agent-execute, send-dispatch-worker, types/sends.ts), disc_profile/conversation_summary (55 refs em src/), zoom_join_url (zoom-upsert-event/index.ts:148,210), leads.control (ai-agent-execute:198,1042), bi_voice_session_log (VoiceChatButton:71), ai_last_message_at (whatsapp/tiktok/meta-inbound) — todos continuam em KEEP, classificação v2.1 íntegra. **3 HIGH** continuam corretamente em INVESTIGATE (lgpd com INSERT em function SQL `20260423004000_lgpd_export.sql:123`; crm_basesconhecimento com 247 refs em migrations exigindo pg_stat_user_tables). **2 LOW remanescentes (não-bloqueantes, herdados do v2):** LOW-2 query pg_trigger específica para contact_attempts em squad-c-prod-validations.md; LOW-3 placeholder `<canonical_users>` em §3.6 antes da Fase 4. Pré-requisitos finais para liberar migrations: anexo de `squad-c-prod-validations.md` com resultados pg_stat_user_tables/pg_policies/pg_trigger cobrindo as 39 tabelas + PO ack (enrichment + USA Q-fields) + Squad B ack (messages.thread_id/tokens_qty) + decisão canônica de usuários (bloqueio Fase 4). **Próximo passo:** @dev-data-engineer escrever queries de §7 cobrindo as 39 tabelas; @dev-devops aguardar PASS final por fase pós-validação prod. Veredicto v2.1 completo em `docs/smart-memory/ops/squad-c-qa-verdict.md` §8 (Apêndice Rodada 3).
- **SQUAD-C-DB-AUDIT v2 (2026-06-08, re-QA)**: PASS (condicional). Squad-c-architect re-submeteu consolidado em status `final-v2` corrigindo integralmente §6.1 e §6.2 do veredicto v1: 7 CRITICAL removidos dos drops (Q-fields BANT snake_case mantidas; disc_*/conversation_summary/service_status mantidas; meetings.zoom_* mantidas; leads.control mantida; bi_voice_*/bi_tiktok_ad_spend/tenant_* reclassificados MANTER no §1.5 com caveat ADR-AUTH-09; score_incomes removida do Tier 1; ai_last_message_at mantida). 3 HIGH movidos para INVESTIGATE com pré-requisitos `pg_stat_user_tables`/`pg_trigger` explicitados (lgpd_anonymization_log, sistema_buffer_agente/_controle_agendamentos, crm_basesconhecimento*). Plano enxuto: Fase 1 (31+5 tabelas) + Fase 2 (15 colunas: 13 CamelCase Q-fields + archived_at + contact_attempts) + Fase 3 (13+4+2 colunas) + Fase 4 (helpers, bloqueado por canônica usuários) + Fase 5 (8 INVESTIGATE legítimos). §11 changelog v1→v2 + lição aprendida sobre cross-ref por COLUNA propagada. Spot-checks adicionais (llm_connections, people, score_income_id, _meta_debug): nenhuma regressão. 3 LOW não-bloqueantes: (LOW-1) llm_connections no §1.4 com decisão ambígua — binarizar antes da migration Fase 1; (LOW-2) query pg_trigger específica para contact_attempts deve ser anexada em squad-c-prod-validations.md; (LOW-3) placeholder `<canonical_users>` em §3.6 precisa ser resolvido antes da Fase 4. Pré-requisitos para liberar migrations: anexo de `squad-c-prod-validations.md` com resultados de pg_stat_user_tables/pg_policies/pg_trigger + PO ack (enrichment + USA Q-fields) + Squad B ack (messages.thread_id/tokens_qty) + decisão canônica de usuários. Próximos passos: @dev-data-engineer escrever queries de §7 e criar squad-c-prod-validations.md; @architect resolver LOW-1; @dev-devops aguardar QA PASS final pós-validação. Veredicto v2 completo em `docs/smart-memory/ops/squad-c-qa-verdict.md`. **Superado pelo v2.1 — ver entrada acima.**
- **SQUAD-C-DB-AUDIT (2026-06-08)**: FAIL (parcial). Squad C consolidado `squad-c-db-final-report.md` propôs ~43 tabelas SAFE_DROP + ~50 colunas mortas em 4 fases de migration. QA gate identificou 7 CRITICAL falsos positivos: (1) Q-fields BANT em `clients_people` (`q1_main_bottleneck`..`q26_disc_analysis`) USADAS em ai-agent-execute, send-dispatch-worker, filter-leads-for-send, dispara-webhook, QualificacaoIASection, PromptShortcutsBar, qFieldLabels, identidadeModels; (2) `clients_people.disc_profile/disc_summary/conversation_summary/service_status` USADAS em ClienteSingle, NegocioSidebar, NegocioSingle, templateUtils, Conversas — não exclusivas de Coach PRO; (3) `meetings.zoom_join_url/zoom_sync_error` USADAS em `supabase/functions/zoom-upsert-event/index.ts`; (4) `leads.control` USADA em ai-agent-execute, sends-import-contacts, useAgenteTeste, useTestSession (core de agentes IA); (5) Tier 4 INVESTIGATE `bi_voice_*`, `bi_tiktok_ad_spend`, `tenant_roles`, `tenant_role_permissions`, `tenant_api_keys` TODAS em uso ativo em usePermissions, useApiKeys, biTools, VoiceChatButton, gemini-ws-proxy, tiktok-ads-sync; (6) `score_incomes` USADA em JOIN em ScoreInformationDisplay.tsx (architect classificou como "versão antiga sem consumidor"); (7) `clients_people.ai_last_message_at` populada por whatsapp-inbound. Issues HIGH: `lgpd_anonymization_log`, `sistema_buffer_agente`, `sistema_controle_agendamentos`, `crm_basesconhecimento*` precisam validação contra `pg_stat_user_tables`/`pg_trigger` antes do DROP. Raiz causal: sub-relatórios `schema-audit.md` (Lyra) e `squad-c-unused-columns.md` (analyst) não cruzaram nomes de COLUNAS contra `src/**` + `supabase/functions/**` antes de classificar como órfãs; architect propagou sem re-validar. Pós-correção, plano enxuto válido: 31 tabelas Tier 1 confirmadas SAFE + 13 colunas CamelCase Q-fields + 13 colunas `leads` (4 órfãs + 9 `old_*`) + 4 colunas `messages` (com sign-off Squad B) + 2 FKs `meetings`. Validações pendentes em prod (`pg_policies`/`pg_indexes`/`pg_stat_user_tables`/`pg_trigger`) seguem obrigatórias antes de qualquer Fase. Devolução para squad-c-architect corrigir o consolidado e re-submeter. Veredicto completo em `docs/smart-memory/ops/squad-c-qa-verdict.md`.
- **SQUAD-B-OMNI-FIXES (Bug 1 + Bug 2 + Hardening, 2026-06-08)**: CONCERNS. Diagnóstico `squad-b-omni-diagnosis.md` apontou (Bug 1) filtro de canal em `get_omni_contacts` RPC + fallback `useConversasSimples.ts:67` exigia `whatsapp|instagram|tiktok` — contatos só-email criados por iscas digitais (ebook/webinar) ficavam invisíveis no `/omni` apesar da row em `messages` existir; agravado por INSERT fire-and-forget em `lp-submit:1042` engolindo erros silenciosamente. (Bug 2) histórico vazio em `ConversaDetalhes` com 4 candidatas — merged orphan, `crm_messages` legado, realtime/polling, RLS silenciosa. **Fixes aplicados em 3 camadas:** (Beta/Rex) migration `20260609100000_fwup31_get_omni_contacts_include_email.sql` `CREATE OR REPLACE` adicionando `OR p.email IS NOT NULL` + `p.email ILIKE '%'||termo||'%'` na busca; `useConversasSimples.ts:79` fallback alinhado com novo filtro; `lp-submit:1063-1106` `await` + `log.error` (em adição ao hardening do delta). (Gamma/Serak) migration `20260608110000_fwup32_reparent_orphan_messages_to_canonical.sql` `UPDATE messages.people_id=p.merged_into_id` para órfãos de merge predating RPC v2; `useMensagensPorPessoa.ts:60-88` resolve cadeia merge em query time (fallback gracioso em erro de lookup); realtime dual-channel; toast `Erro ao carregar mensagens` throttled 1×/15s. (Delta/Kron) `lp-submit:1063-1085` `Promise.race` timeout 5s + retry único + telemetria escalonada WARN→ERROR + caps 200/500/30 contra DoS via payload; `useMensagensPorPessoa.ts:60-88, 227-294` cadeia recursiva 5 níveis com ciclo guard tanto na query quanto no realtime (dual-channel virou N-channel); `useMensagensPorPessoa.ts:168-173` polling exponential backoff 5s→60s baseado em `fetchFailureCount`; `useConversasSimples.ts:8-14, 104` `sanitizeSearchTerm` (remove `, ( ) * \ :` + cap 100) e regex ISO em `filtroData`; `ConversaDetalhes.tsx:198-213` 3 estados distintos (loading, error+retry button, empty). **8/8 checklist:** (1) code review PASS — comentários explicam *why* (cap 500 contra metadata bloat, timeout contra Edge hang, ciclo guard, etc); (2) testes N/A — projeto não tem suite para hooks tocados (estado pré-existente, dívida registrada); (3) ACs PASS — todos endereçados; (4) regressões PASS — `tsc --noEmit` exit 0; (5) performance CONCERN — lookup `clients_people` extra por mount de `ConversaDetalhes` (1 query em pessoa não-merged, até 5 sequenciais em merge chain profunda); (6) security PASS — sanitização PostgREST + caps DoS + `SECURITY INVOKER` mantido; (7) doc PASS — `squad-b-fixes.md` detalhado por camada; (8) contratos PASS — RPC mantém assinatura idêntica. **Lint validado via `git stash --keep-index -u`:** 15 erros pré-existentes em fc3f9217, 15 erros pós-fix, **delta 0** — alegação do Kron confirmada bit-a-bit. **CONCERN HIGH-1:** `fwup32` faz UPDATE single-pass — cadeias A→B→C ficam com mensagens em B (status=merged) após o pass. Mitigado em runtime pelo hook recursivo (UX final correta), mas queries diretas em SQL (BI/exports) ainda veem dados em intermediário. Patch sugerido: WHILE FOUND loop para multi-pass. **CONCERN HIGH-2:** realtime dual-channel pode abrir até 5 channels por pessoa selecionada em merge chains profundas — pressão potencial sobre limite WebSocket Supabase (~100/cliente). Cleanup robusto + `cancelled` flag + Set `visited` mitigam. Trade-off aceitável (chains profundas são raras). **CONCERN MEDIUM-1:** `lp-submit:1032` `omniChannel = pessoa.whatsapp ? 'whatsapp' : pessoa.email ? 'email' : 'whatsapp'` — caso "sem nada" cai em `'whatsapp'`, semanticamente errado. Sugestão de canal virtual `'form'` fora de escopo do hotfix (registrado em `[[../project/modules/form-pro-lp]]` follow-up). **Próximo passo:** `@dev-devops` push liberado. Migrations a aplicar no tenant João Guirunas: fwup31 + fwup32 (idealmente patchado multi-pass). Edge fn `lp-submit` precisa redeploy. Frontend deploy padrão. Veredicto completo em `docs/smart-memory/ops/squad-b-qa-verdict.md` com checklist manual pós-deploy.
- **WEBHOOK-TEMPLATE-FIX (`webhook-inbound/index.ts` linha 267 + comentário L266)**: PASS. Fix de 1 linha — `source_type: 'webhook'` → `source_type: 'form'` — corrige bug latente introduzido em `7622e152` (feat webhook increment). **Causa raiz (validada in-place):** constraint `messages_source_type_check` no banco live (verificada via `supabase db query --linked` em 2026-05-10) é `CHECK (source_type = ANY (ARRAY['inbound','manual','ai_agent','campaign','form','followup','appointment_reminder']))` — `'webhook'` NÃO consta. Resultado runtime: INSERT em `messages` em `maybeDispatchTrigger` (L259-285) sempre falhava com Postgres CHECK violation; o `if (msgErr)` em L287-293 capturava, logava `webhook_trigger.insert_failed`, retornava `false`, e o response do endpoint trazia `trigger_dispatched: false` mesmo com webhook configurado e ativo. Usuário relatou exatamente esse sintoma (configurou template, disparo não rolou). **Fix:** L267 muda string para `'form'` — mesmo precedente que `lp-submit/index.ts:1036, 1208, 1250` usa para o mesmíssimo padrão (template WA enfileirado pós-form-submit com `from_contact='sistema'` e body pré-renderizado). Discriminação real da origem permanece intacta via `module_ref_id: wh.id` (L272) apontando para `inbound_webhooks.id`. Comentário inline em L266 ancora a decisão para evitar regressão futura. **8/8 checklist:** (1) code review — cirúrgico, sem refactor adjacente; (2) testes — escopo de 1 linha não justifica novos unit tests, integration depende de WA channel real; (3) ACs — webhook com `trigger_config.enabled=true` agora consegue enfileirar `messages` row, satisfaz objetivo do `maybeDispatchTrigger`; (4) sem regressão — `useCanSendMessage.ts:173-181` exclui `source_type='form'` da janela 24h **apenas quando `from_contact='cliente'`** (L179); a row criada pelo trigger tem `from_contact='sistema'` (L265 do edge fn), então a query do hook não a captura → janela 24h continua sendo calculada por inbound reais; (5) performance idem; (6) segurança idem — sem mudança em superfície de input externo (`create_mode` continua vindo do DB, whitelist defensiva, etc.); (7) doc — comentário inline auto-documenta o porquê; (8) contratos — response body do webhook e schema `inbound_webhooks` inalterados. **Casos de borda revisados:** (a) `trigger_config.enabled=true` mas `wa_template_id` excluído do DB → guard L204-209 `webhook_trigger.template_not_found` dispara, retorna false sem INSERT — best-effort preservado. (b) Webhook chamado sem campo whatsapp no payload → guard L173-175 `webhook_trigger.no_whatsapp`, sem INSERT. (c) `action === 'skipped'` (linha 508) — correto NÃO disparar: significa que nada foi criado/atualizado no CRM, então não há lead para mandar template para. (d) `create_mode='criar'|'criar_se_nao_existir'|'atualizar_etapa'|'somente_etapa'` — fluxo bifurcado anterior (L426-501) intacto; `maybeDispatchTrigger` só é chamado quando `action !== 'skipped'` (L508). (e) UI conversas — `MessageContent.tsx:234` renderiza `isFormMessage` para qualquer `source_type='form'` (não só do lp-submit), o que pinta o card com prancheta + "Ver X campos preenchidos" mostrando "Dados do formulário não armazenados". Pré-existente (lp-submit já faz o mesmo INSERT) — não é regressão deste fix, é status quo, mas vale registrar como tech-debt UX se UI precisar diferenciar form-submit vs webhook-trigger no futuro. **Mea culpa retrospectivo:** gate WEBHOOK-INCREMENT anterior (`results.md` linha 93, 2026-05-09) verificou ACs/contratos/lint/types mas NÃO cruzou `source_type: 'webhook'` contra a constraint live → bug entrou em prod. `feedback_runtime_smoke_before_gate.md` (lição de Coach Pro CP-1..7 round 1) explicitamente cobre esse gap: gates de UI/runtime exigem smoke com dados reais, não só contract de tipos. Aplicado neste gate via `supabase db query --linked` direto na constraint. **Trigger_config audit (dev-data-engineer):** webhook de teste tem `enabled=true`, `wa_channel_id` e `wa_template_id` válidos no DB; falha não era falta de config, era CHECK violation no INSERT. **Push LIBERADO** — `@dev-devops` pode commitar o fix (1 arquivo, 2 linhas). Não exige migration porque a constraint `messages_source_type_check` permanece como está; mudar para incluir `'webhook'` seria semanticamente incorreto (origem já discriminada por `module_ref_id`).
